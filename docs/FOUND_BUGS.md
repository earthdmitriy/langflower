# Found bugs log

Append-only record of **reproduced bugs** (fixed or open). Used at **retrospective**
to spot recurring design flaws — not for product backlog or feature requests.

**Do not use this file for:** ADRs (see [ADR.md](ADR.md)), long-horizon goals
([TBD.md](TBD.md)), implementation checklists
([DONE/EPICS/README.md](DONE/EPICS/README.md)), or canvas-only incidents
([`packages/ui/docs/DIAGRAM_CANVAS.md`](../packages/ui/docs/DIAGRAM_CANVAS.md) —
link from here when a canvas bug also implies a cross-cutting flaw).

---

## When to log

Log an entry when **any** of the following is true:

1. A user-reported or agent-found bug was **reproduced** locally.
2. A fix landed and the root cause was **non-obvious** (not a typo or missing import).
3. The bug reveals a **design assumption** that was wrong (state ownership, scope,
   lifecycle, concurrency, etc.).

**Who:** agents and humans — add the entry in the **same PR** as the fix when possible,
or immediately after repro if the fix is deferred.

**Retrospective:** skim this file for repeated _design flaw signals_ (same theme across
entries) before large refactors or new ADRs.

---

## Entry template

Copy for each new bug:

```markdown
### BUG-YYYY-MM-DD — short title

| Field                  | Value                                                       |
| ---------------------- | ----------------------------------------------------------- |
| **Date**               | YYYY-MM-DD                                                  |
| **Area**               | execution · ui · server · shared · canvas · ws · hitl · …   |
| **Status**             | fixed · open · wontfix                                      |
| **Symptom**            | What the user saw                                           |
| **Repro**              | Minimal steps                                               |
| **Root cause**         | Technical why                                               |
| **Fix**                | Files / behaviour change (PR or commit ref if available)    |
| **Design flaw signal** | What the architecture got wrong; candidate ADR or principle |
| **Regression test**    | Test path, or `none` + reason                               |
```

---

## Log

Newest first.

### BUG-2026-08-30 — Product CLI bundle: dynamic require of `node:events`

| Field                  | Value                                                                                                                                                                           |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-08-30                                                                                                                                                                      |
| **Area**               | cli · release bundle                                                                                                                                                            |
| **Status**             | fixed                                                                                                                                                                           |
| **Symptom**            | Global `langflower` printed `Starting Langflower...` then crashed: `Error: Dynamic require of "node:events" is not supported` in `dist/chunk-*.js` (commander).                 |
| **Repro**              | `npm run install-local` after epic 44 phase 3; run `langflower`.                                                                                                                |
| **Root cause**         | esbuild ESM output wraps CJS `require` in a shim that throws when `require` is missing. Bundled `commander` (CJS) calls `require('node:events')`.                               |
| **Fix**                | `createRequire(import.meta.url)` banner on product chunks (`build/lib/bundle-product.mjs`).                                                                                     |
| **Design flaw signal** | Concatenating CJS Node libraries into ESM needs a real `require` (or leaving those packages external). A static grep of externals does not catch runtime `require` of builtins. |
| **Regression test**    | `tests/unit/release/bundle-product.test.ts` — spawn bundled `index.js --help`                                                                                                   |

### BUG-2026-08-27 — `pipeValue` + async never pending; chrome stayed green on later waits

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Date**               | 2026-08-27                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Area**               | common-nodes · node-sdk · canvas · ws                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Symptom**            | Delay (and other catalog waits: files, HTTP, LLM feedback turns, HITL Review Gate) stayed **green** after the first success. Repeat→Delay ticks 2–N never showed working chrome. `runner.port` had no `{ pending: true }` on those activations.                                                                                                                                                                                                                                |
| **Repro**              | Number → Repeat(count 4) → Delay(3000ms) → Repeat.trigger. Tick 1 goes amber then green; ticks 2–4 stay green for the whole wait. Same for LLM turn 2 after a finished `result`.                                                                                                                                                                                                                                                                                               |
| **Root cause**         | `pipeValue` forwards loading but **does not create it**. `concatMap(delay)` / `from(Promise)` / HTTP / `switchMap(runSession)` only delay the success lane. Canvas `appendNodeChromeFrame` also ignored **output** `{ pending: true }` after a settled value (`hasNonStreamingValue` stayed true), so even a correct pending would have left the node green.                                                                                                                   |
| **Fix**                | SDK `withLoading()` on the raw SO stream before async `pipeValue` (Delay / files / crawl / embed / MCP); `defineNode` bakes the same stamp before `execute`; LLM cycle pending **per turn** via `statefulObservable` + `concatMap` (not `pipeValue`, which would cancel in-flight turns); Sub-Agent pending **per invoke** (not per token); Review Gate `response`/`feedback` pending until HITL. Chrome fold clears settled green on `out` pending. Feed still drops pending. |
| **Design flaw signal** | Async wait is not a value transform. Loading belongs on `.pipe(withLoading())` before `pipeValue` work (timer / HTTP / HITL wait), or on a queued `concatMap` loader for LLM turns — not on the chunk stream (that re-pends every token through demux). UI folds that treat “has a value” as terminal miss later waits on the same port.                                                                                                                                       |
| **Regression test**    | `packages/common-nodes/src/flow/delay/node.test.ts`; `packages/node-sdk/.../define-node/test/samples/samples.test.ts` (async `execute` pending); `packages/ui/.../canvas-node-status-projection.test.ts` (out pending after green); `packages/common-nodes/src/ai/features/llm-session/llm-session-shell.test.ts`; `packages/common-nodes/src/hitl/review-gate/node.test.ts`; `tests/integration/ws/pending-events-bridge.ws.test.ts` (pending after Delay `in` value)         |

### BUG-2026-08-26b — OpenAI SDK embeddings default base64 unwrap zeros local vectors

| Field                  | Value                                                                                                                                                                                                                                                                                                                              |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-08-26                                                                                                                                                                                                                                                                                                                         |
| **Area**               | common-nodes · embeddings                                                                                                                                                                                                                                                                                                          |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                              |
| **Symptom**            | hello-embed ingest completed; sqlite stored dim-256 vectors with L2-norm 0. KB search returned arbitrary top-K hits at `score: 0.00`.                                                                                                                                                                                              |
| **Repro**              | Settings default embedding model (local OpenAI-compatible). Run **KB ingest**, then **KB manual search**. First ingest progress dump: `rawCtor: Float32Array`, `rawLen: 256`, `rawNorm: 0`, `embedTextLen: 447`.                                                                                                                   |
| **Root cause**         | openai-node `embeddings.create` without `encoding_format` defaults to `base64` (PR 1312) and then `toFloat32Array` on the body. Local servers ignore the param and return JSON `number[]`; the unwrap still runs and yields a zero typed array. Chat `completions.create` has no such field. Text and `passage:` prefix were fine. |
| **Fix**                | Always pass `encoding_format: 'float'` so the SDK returns the JSON array as-is. Keep `toVectors` (numeric array / non-zero typed array). Zero-norm vectors throw.                                                                                                                                                                  |
| **Design flaw signal** | Embeddings are not chat: the SDK’s default encoding path is a second contract. Call embeddings with an explicit `encoding_format`; treat a zero vector as a hard failure, not a valid dim probe.                                                                                                                                   |
| **Regression test**    | `packages/common-nodes/src/embeddings/create-embedding.test.ts` — `encoding_format: 'float'` on create body; reject `{ length }` objects; reject zero-norm vectors                                                                                                                                                                 |

### BUG-2026-08-26 — hello-embed `.ts` imports failed compile; load repair stripped kb-ingest

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                             |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-08-26                                                                                                                                                                                                                                                                                                                                                        |
| **Area**               | compiler · custom-nodes · editor                                                                                                                                                                                                                                                                                                                                  |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                             |
| **Symptom**            | Opening **KB ingest** showed “Loaded with 1 node(s) / 3 edge(s) removed (invalid)”. Canvas kept only Boolean, embed-provider, Finish.                                                                                                                                                                                                                             |
| **Repro**              | Seed `hello-embed` with `from './lib/x.ts'` and pack `tsconfig.json` **without** `allowImportingTsExtensions`. `langflower start`, load `kb-ingest`. Pack writes `COMPILATION_ERRORS.md`; `hello-embed-ingest` never registers.                                                                                                                                   |
| **Root cause**         | Pack compile runs `tsc --noEmit` from pack tsconfig (NodeNext). `.ts` import suffixes are illegal unless `allowImportingTsExtensions` (and `noEmit`). Failed entries never enter the palette. Workflow load repair treats missing custom `type`s as unknown and strips nodes/edges. hello-embed copied the `.ts` import style without the matching tsconfig flag. |
| **Fix**                | hello-embed `tsconfig.json`: `allowImportingTsExtensions: true`. Compiler tests compile `.ts`-suffix imports and the skeleton hello-embed pack.                                                                                                                                                                                                                   |
| **Design flaw signal** | Compile failure and workflow “invalid node” UX are the same event. Authors/helpers must read `COMPILATION_ERRORS.md` before treating load repair as a bad JSON graph. Seed packs that use `.ts` imports must ship the matching tsconfig flag.                                                                                                                     |
| **Regression test**    | `packages/compiler/src/compile-project-nodes.test.ts` — internal `.ts` imports + skeleton `hello-embed` pack compile                                                                                                                                                                                                                                              |

### BUG-2026-08-18d — Live canvas chrome wiped when `runner.started` arrived after ports

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-08-18                                                                                                                                                                                                                                                                                                                                                                    |
| **Area**               | runtime · ui · ws                                                                                                                                                                                                                                                                                                                                                             |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                                         |
| **Symptom**            | Run test review-gate, Stop while the gate is still pending: canvas went idle (no amber). Refresh replayed the snapshot and showed pending chrome. Feed/chrome treated `runner.started` as a new run and wiped live prefix frames.                                                                                                                                             |
| **Repro**              | Start a graph with a long pending node. NDJSON: `runner.port` `{ pending: true }` then `runner.started`. Stop before the node settles. Live canvas idle; reload shows amber.                                                                                                                                                                                                  |
| **Root cause**         | `RuntimeRunner.runScope` wired the graph (port taps fire) **then** returned `runId`. The server emits `runner.started` only after `start()` returns, so ports raced ahead. UI folds keyed a new-run wipe on `started` when `runId` was still `null` (port 7-tuples carry no runId). Snapshot replay has no that wipe.                                                         |
| **Fix**                | `runScope` assigns `runId` and `'running'` synchronously, then `queueMicrotask` → `wireScope`. `interrupt` / `dispose` cancel a pending wire. Callers keep `start()` then `bridgeEmit(started)`. Chrome / HITL / feed reset **adopts** `null → runId` instead of clearing.                                                                                                    |
| **Design flaw signal** | Returning from `start()` after side-effect telemetry makes every `started`-after-`start()` caller lose the fence. Run identity must exist before the first port, and live folds must adopt an unknown `runId` instead of treating `started` as “wipe and begin”.                                                                                                              |
| **Regression test**    | `packages/runtime/src/testing/workflows/start-wire-defer.workflow.test.ts`; `tests/integration/ws/pending-events-bridge.ws.test.ts` (`started` before first `port`); `execution-chrome-fold.test.ts` / `canvas-node-status-projection.test.ts` / `canvas-node-hitl-projection.test.ts` / `execution-feed.service-lifecycle.test.ts` (ports then reset `null→id` keep pending) |

### BUG-2026-08-18c — Pending/value canvas chrome invisible after ResponseDto pulse

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-08-18                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Area**               | ui · canvas                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Symptom**            | Starting a graph such as string → review-gate: short green blinks on both nodes, then no colour until the gate finished. NDJSON showed `runner.port` `{ pending: true }` on string `value` and gate `ok`/`fail`.                                                                                                                                                                                                                                                              |
| **Repro**              | Run test review-gate. Watch canvas: pulse, then idle borders/wires, then green when `ok` or `fail` emits `{ value }`.                                                                                                                                                                                                                                                                                                                                                         |
| **Root cause**         | Folds already stored ResponseDto (`'pending' in` / `'value' in`). Paint lost: (1) ng-diagram path reads `--edge-stroke`, but pending/value/error only set `path { stroke }` in `styles.scss`; pulse animation ended at idle gray `#a1a1aa`. (2) node `--pending`/`--value` `border-color` lost to Tailwind `border-zinc-300` / `dark:border-zinc-700`. (3) node `pulse$` treated input `{ value }` as a delivered output, so the gate flashed green instead of staying amber. |
| **Fix**                | Host `--edge-stroke` / `--ngd-default-edge-stroke` on `lf-edge--pending\|value\|error` (same as selected). Pulse 100% fades to `var(--edge-stroke)`. Idle/status node `border-color` only in `node-port-layout.css`. Node pulse is `out` + `'value' in` DTO.                                                                                                                                                                                                                  |
| **Design flaw signal** | Classes on the host are not paint. Custom edge templates must set the CSS variables the path actually reads. Status colour cannot share an element with utility `border-*` colours. Pulse is an output-value flash, not “any `{ value }` on the node”.                                                                                                                                                                                                                        |
| **Regression test**    | `canvas-node-status-projection.test.ts` (string value / gate pending); `execution-chrome-fold.test.ts` (trigger value + ok/fail pending); `lf-edge-chrome.test.ts` (`--edge-stroke` on pending/value); `lf-node-chrome.test.ts` (no pulse on input value or output pending)                                                                                                                                                                                                   |

### BUG-2026-08-18b — Sub-Agent work log filled with literal `null`

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-08-18                                                                                                                                                                                                                                                                                                                                                             |
| **Area**               | ui · common-nodes                                                                                                                                                                                                                                                                                                                                                      |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                                  |
| **Symptom**            | Researcher Sub-Agent visit showed repeating Tool / Reasoning / DRAFT cards whose bodies were the string `null` (e.g. `Reasoning: context null`).                                                                                                                                                                                                                       |
| **Repro**              | Plan → Researcher. Ask Plan to delegate. Watch the Researcher work-log card while it streams.                                                                                                                                                                                                                                                                          |
| **Root cause**         | Sub-Agent wrapped a chunk `Subject` in `statefulObservable({ input })`. Each token re-emits a loading sentinel; `pipeValue` passes loading to **every** demuxed port. Runtime telemetry is `pending`+`null` (JSON has no `undefined`). Fold only dropped `pending`+`undefined`, so growing-merge appended `"null"` and sibling ports opened empty Tool/Draft segments. |
| **Fix**                | Drive the specialist cycle with `context$.pipeValue(switchMap(() => chunks$))` (inner chunks are values, not per-token loads). Fold also drops `pending`+`null`.                                                                                                                                                                                                       |
| **Design flaw signal** | `statefulObservable({ input: chunks$ })` is not a paced session. Loading is not a feed value; WS/JSON pending is `null`, not `undefined`.                                                                                                                                                                                                                              |
| **Regression test**    | `packages/ui/src/app/features/feed-folding/tests/execution-feed.service-inputs.test.ts` (pending null); `packages/common-nodes/src/ai/nodes/sub-agent/node.test.ts` (no sibling pending per chunk)                                                                                                                                                                     |

### BUG-2026-08-18 — Parent feed visit stayed open across Sub-Agent

| Field                  | Value                                                                                                                                                                                                                                         |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-08-18                                                                                                                                                                                                                                    |
| **Area**               | ui · execution · common-nodes                                                                                                                                                                                                                 |
| **Status**             | fixed                                                                                                                                                                                                                                         |
| **Symptom**            | Calling a Sub-Agent did not close the parent agent visit. After Writer finished, parent reasoning/draft appended into the old card (scrolled off-screen above Writer).                                                                        |
| **Repro**              | Helper → Writer. Ask Helper to delegate a task. Watch the work log: Helper stays `working…`; Writer streams below; Helper continuation grows the first Helper card.                                                                           |
| **Root cause**         | `toolLog` is `feed.streaming: true`, so the `→ Writer` frame kept the parent visit open (concurrent-streaming while-open). Live Sub-Agent chunks during `invoke` then made that open visit sit above the specialist card.                     |
| **Fix**                | Fold: a `common-sub-agent` frame stamps `closesPreviousVisit` and closes the previous visit (different `nodeId`). Production `→ Researcher_subagent(` toolLog does not close. Ordinary tools unchanged.                                       |
| **Design flaw signal** | Streaming on a port is a visit policy, not only a UI grow-merge hint. A canvas specialist is a new visit, not an in-card tool row. Visit close belongs in the fold from **node type**, not a toolLog regex or per-event feed override on `T`. |
| **Regression test**    | `packages/ui/src/app/features/feed-folding/tests/execution-feed.service-visits.test.ts` (Sub-Agent node type vs ordinary tool)                                                                                                                |

### BUG-2026-08-17 — Sub-Agent invoke returned a silent empty string

| Field                  | Value                                                                                                                                                                                                                                                                                                                                 |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-08-17                                                                                                                                                                                                                                                                                                                            |
| **Area**               | execution · common-nodes                                                                                                                                                                                                                                                                                                              |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                 |
| **Symptom**            | Parent agent could call the Writer Sub-Agent tool; `invoke` came back as `''` with no `Error:` / timeout text. Two argument shapes, same no-op.                                                                                                                                                                                       |
| **Repro**              | Starter graph Helper → Writer. Ask Helper to ping Writer with a trivial task. Writer `toolLog` showed the call; the tool result was empty.                                                                                                                                                                                            |
| **Root cause**         | Sub-Agent RPC was `Subject.next(InvokeTurn)` into `createLlmSessionCycle$` (hot Subject + `switchMap` on `context$`). Completing the waiter copied inner `{ kind: 'response', text }` even when that text was `''` (reasoning-only streams, empty `done`, no draft). A dropped `next()` hung until timeout instead of failing closed. |
| **Fix**                | `enqueueInvoke` runs `runSubAgentTurn` directly (serial queue, local history). Feed ports still demux a chunk `Subject`. `invokeDone` uses response, else last draft, else last reasoning; still blank → `Error: Sub-Agent returned no content`. `refreshLiveWiredToolPacks` also peeks `subagent-registration`.                      |
| **Design flaw signal** | Do not use a hot Subject + feedback session machine as an RPC. Do not treat empty `response` as a successful tool result.                                                                                                                                                                                                             |
| **Regression test**    | `packages/common-nodes/src/ai/nodes/sub-agent/node.test.ts` (empty `done`; reasoning-only then empty `done`); `packages/server/src/bridge/get-live-wired-tools.test.ts`                                                                                                                                                               |

### BUG-2026-08-16 — Frozen `context.tools` after custom hot-swap

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-08-16                                                                                                                                                                                                                                                                                                                                                                    |
| **Area**               | execution · common-nodes · server                                                                                                                                                                                                                                                                                                                                             |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                                         |
| **Symptom**            | After `compile_custom_nodes` hot-swapped a custom `defineToolRegistrations` node, the next `invoke` in the **same** agent turn still ran the previous handler and omitted brand-new `toolId`s.                                                                                                                                                                                |
| **Repro**              | Wire custom tools + compile into Fake-LLM; disk v1 → start → write v2 → scripted `compile_custom_nodes` then custom `toolId`; invoke returned v1.                                                                                                                                                                                                                             |
| **Root cause**         | `runAgentLoop` / `runLlmLoop` froze `tools` from `combineInputs`. After `swapNode`, the LLM `tools` input stayed subscribed to the pre-swap instance (`wireScope` copied `nodesById` at `start()`). Re-reading `context.tools` stayed stale. Pushing a new `tools` port value would `switchMap`-reset ADR-016.                                                                |
| **Fix**                | `getLiveWiredTools` walks current editor inbound `tools` edges (one-shot `value$` subscribe, no LLM input rewire). `getTools` re-reads that list each provider round and `invoke`. Recompile is a wired Langflower Tools bus RPC (`customPalette.update.requested`). Agent `toolCtx` is identity + permission fields only — `buildAgentToolCtx` does not attach the host bag. |
| **Design flaw signal** | A port snapshot is not live inventory across hot-swap; editor `getNode` identity after `swapNode` is not the `wireScope` instance frozen at run start. Ambient host-bag compile would let any wired handler recompile without the tools edge.                                                                                                                                 |
| **Regression test**    | `packages/common-nodes/src/ai/features/llm-loop/run-agent-loop.test.ts` (getTools v1 then v2); `tests/integration/ws/custom-palette-compile-tool.ws.test.ts` (same-run custom invoke returns v2)                                                                                                                                                                              |

### BUG-2026-08-06 — Agent `grep` froze the server (Node walk on main thread)

| Field                  | Value                                                                                                                                                                                                                      |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-08-06                                                                                                                                                                                                                 |
| **Area**               | tools · execution · server                                                                                                                                                                                                 |
| **Status**             | fixed                                                                                                                                                                                                                      |
| **Symptom**            | After `→ grep …` on Plan `toolLog`, HTTP/WS stopped responding for minutes; later `← grep: Error: Tool grep timed out after 60000ms` with wall clock ≫ 60s.                                                                |
| **Repro**              | Run Plan/agent with builtin `grep` over a large tree / heavy regex; observe server unresponsive until JS yields, then RxJS tool timeout.                                                                                   |
| **Root cause**         | Builtin `grep` used an in-process Node walk + sync `regex.test` per line on the main server event loop. `toolTimeoutMs` only unsubscribed the Observable and could not fire (or abort work) while JS was stuck.            |
| **Fix**                | ts-scan-style cascade: async `rg` → async `grep` → bounded Node walk (yields, file/byte/line caps, symlink skip); thread `AbortSignal` from `runLlmLoop` tool timeout/cancel through harness to kill children / stop walk. |
| **Design flaw signal** | Heavy project I/O must not pin the Node event loop; Observable timeout is not cancellation — builtins need AbortSignal (or a child process that can be killed).                                                            |
| **Regression test**    | `packages/tools/src/builtins/grep/search.test.ts`                                                                                                                                                                          |

### BUG-2026-08-04 — Empty `model` paints first catalog label

| Field                  | Value                                                                                                                                                                                           |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-08-04                                                                                                                                                                                      |
| **Area**               | ui · config                                                                                                                                                                                     |
| **Status**             | fixed                                                                                                                                                                                           |
| **Symptom**            | Inspector Model showed a live catalog id (e.g. gemma) while `params.model` stayed `""`; Review threw `Model is required for Review chat`. Switching model away and back wrote a real id.        |
| **Repro**              | Skeleton / `kb-create` Review node with `providerId: "local"` and `model: ""`; open Inspector with a populated models catalog.                                                                  |
| **Root cause**         | `withSelectEmptyOption` prepended `Select…` for providers/skills but not `langflower.models`. With `value === ""`, no `<option>` got `[selected]`; the browser painted the first catalog entry. |
| **Fix**                | Prepend blank `Select…` for `langflower.models` (same as providers). No sole-model auto-bind (live catalogs can briefly show one entry).                                                        |
| **Design flaw signal** | Same as BUG-2026-07-25d: unset catalog selects need an explicit blank option; never trust the browser’s first-option paint as the stored value.                                                 |
| **Regression test**    | `packages/ui/src/app/features/sidebar/tests/select-empty-option.test.ts` (`langflower.models` Select…)                                                                                          |

### BUG-2026-07-30 — Permission-ask amber cue stayed after Allow

| Field                  | Value                                                                                                                                                                                              |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-30                                                                                                                                                                                         |
| **Area**               | ui · hitl · feed                                                                                                                                                                                   |
| **Status**             | fixed                                                                                                                                                                                              |
| **Symptom**            | Yellow `Permission: …` work-log cue remained after the user clicked Allow                                                                                                                          |
| **Repro**              | Run agent → tool permission ask → Allow in composer → cue still visible in feed                                                                                                                    |
| **Root cause**         | Two independent UI folds cleared on local `permissionResolveLocal$`; the client could hide an ask before the server accepted a valid reply, and tabs could diverge.                                |
| **Fix**                | Server broadcasts `runner.permission.accepted` only after `PendingPermissionAsks.reply()` consumes a valid pending ask. Feed and composer pending folds remove the matching ask only on that fact. |
| **Design flaw signal** | UI state for a server-owned gate must reduce authoritative bridge facts, not optimistic local requests.                                                                                            |
| **Regression test**    | `packages/ui/src/app/services/tests/execution-permission-fold.test.ts`, `packages/ui/src/app/features/sidebar/tests/feed-timeline.test.ts`                                                         |

### BUG-2026-07-29 — Canvas label-only flash + compile-on-every-connect

| Field                  | Value                                                                                                                                                                                                                                                                                                              |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Date**               | 2026-07-29                                                                                                                                                                                                                                                                                                         |
| **Area**               | canvas · palette · server · node-sdk                                                                                                                                                                                                                                                                               |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                              |
| **Symptom**            | On load, canvas nodes showed labels only for ~1s before ports/inline appeared. WS log had almost no gap between `workflow.current.snapshot` and `palette.snapshot`, but `customPalette` went `compiling` → `ok` (~1.1s) and `palette.snapshot` was ~376 KB with nested RxJS Observable junk under `inferTypeFrom`. |
| **Repro**              | Start Langflower with custom packs; refresh the editor; watch node bodies and WS frames.                                                                                                                                                                                                                           |
| **Root cause**         | (1) Connect bootstrap always `compilingSnapshot` + `compileProjectNodes` per client. (2) `configureOutput` stored live `inferTypeFrom` Observables on port meta; `toPaletteDefinition` only stripped `getInstance`. (3) Canvas `startWith` empty palette painted label-only nodes before real catalogs.            |
| **Fix**                | Compile custom packs once in `createServer` before listen; connect emits warm `getSnapshot` only. Omit `inferTypeFrom` from stored output meta + strip in `toPaletteDefinition`. Remove canvas palette `startWith`s so `combineLatest` waits for real snapshots.                                                   |
| **Design flaw signal** | Connect treated as a compile trigger; probe handles left on serializable port meta; invented empty catalogs via `startWith` instead of waiting for readiness.                                                                                                                                                      |
| **Regression test**    | `packages/node-sdk/.../samples.test.ts` (JSON-safe passthrough meta); `packages/server/src/palette/palette.service.test.ts` (no Observable junk / compact palette JSON)                                                                                                                                            |

### BUG-2026-07-28b — Skeleton `agents-dialog` load silently no-op'd

| Field                  | Value                                                                                                                                                                                                                                 |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-28                                                                                                                                                                                                                            |
| **Area**               | ws · server · common-nodes                                                                                                                                                                                                            |
| **Status**             | fixed                                                                                                                                                                                                                                 |
| **Symptom**            | Loading `agents-dialog` from the catalog left the prior workflow active; WS showed only `workflow.current.snapshot` of the old id — no error.                                                                                         |
| **Repro**              | Open a project with skeleton samples; load `agents-dialog` while another workflow is active.                                                                                                                                          |
| **Root cause**         | (1) LLM `feedback` was unintentionally single-slot; two edges both targeting `proposer.feedback[0]` failed `addEdge` occupancy. (2) `workflow.load.requested` discarded bind/load Results and always re-emitted the current snapshot. |
| **Fix**                | Restore `feedback` `multi: 'merge'`; distinct slots in `agents-dialog`; emit unicast `workflow.load.failed`; activate rolls back previous bind on failure.                                                                            |
| **Design flaw signal** | Multi fan-in needs `multi` + distinct slots in persisted graphs; expected failures must surface as bus facts (`Result` → client event), not silent success-shaped snapshots.                                                          |
| **Regression test**    | `packages/common-nodes/src/ai/nodes/openai-llm/node.test.ts` (dual feedback slots); load Result path in `load-workflow-into-session` / handler.                                                                                       |

### BUG-2026-07-28 — Custom peer-only pack load fails without project node_modules

| Field                  | Value                                                                                                                                                                                                                                                                 |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-28                                                                                                                                                                                                                                                            |
| **Area**               | compiler · custom-nodes                                                                                                                                                                                                                                               |
| **Status**             | fixed                                                                                                                                                                                                                                                                 |
| **Symptom**            | After `langflower start` in an empty folder, seed `my-nodes` entries failed with `Failed to load bundled module: Cannot find package '@langflower/node-sdk'` (and rxjs / stateful-observable).                                                                        |
| **Repro**              | Empty project (no `node_modules`); seeded `.langflower/nodes/my-nodes` peer-only pack; Custom → Update / `compileProjectNodes`.                                                                                                                                       |
| **Root cause**         | esbuild left host peers as bare externals; native `import()` of `.cache/nodes/.../*.mjs` resolved from the cache path / project tree. Typecheck already used compiler-host peers (BUG-2026-07-24); runtime load did not. Vitest aliases masked the gap in unit tests. |
| **Fix**                | `bundle-pack.ts` rewrites host peer imports to absolute `file://` URLs from `resolveHostPackageEntry`; cache key includes host runtime stamp + policy id.                                                                                                             |
| **Design flaw signal** | Closing host resolution for **tsc only** is incomplete — any phase that emits a loadable artifact must apply the same host-peer contract, and tests must exercise native Node import without Vitest aliases.                                                          |
| **Regression test**    | `compile-project-nodes.test.ts` — peer-only pack outfile has no bare peer imports + subprocess `node` import; `resolve-host-types.test.ts` — `resolveHostPackageEntry`                                                                                                |

### BUG-2026-07-27b — `finish_reason=length` and opaque errors looked finished or dead

| Field                  | Value                                                                                                                                                                                                                                                                        |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-27                                                                                                                                                                                                                                                                   |
| **Area**               | common-nodes · LLM · provider recovery                                                                                                                                                                                                                                       |
| **Status**             | fixed                                                                                                                                                                                                                                                                        |
| **Symptom**            | LM Studio could end a stream with `finish_reason: "length"` and the loop treated it as a normal done (or stalled without operator recourse). Opaque unclassified throws were `recoverable: false`, so the cycle died with little Steer path.                                 |
| **Repro**              | OpenAI-compatible stream whose last choice has `finish_reason: "length"` (possibly truncated tool JSON); or throw an unclassified Error from the factory with `maxTransientRetries: 0`.                                                                                      |
| **Root cause**         | Stream adapter ignored `finish_reason`; classifier marked `unknown` (and stream-without-done protocol) as non-recoverable, so failures either looked like success or hard-stopped the Observable.                                                                            |
| **Fix**                | Plumb `finishReason` on `done` / `provider.done`; treat length / incomplete tool JSON as `output-truncation` (no tool run; compact+retry or Steer); mark `unknown` and stream-without-done as recoverable with mandatory `toolLog` then retry/Steer. Auth/config stay fatal. |
| **Design flaw signal** | **Silent success on truncated provider output** and **opaque failure as Observable death** — anything not clearly auth/config must surface diagnostics and enter retry → Steer, never silent complete or silent kill.                                                        |
| **Regression test**    | `ai/features/openai/create-chat-completion-stream.test.ts` (`mapFinishReason`), `ai/features/llm-loop/run-agent-loop.test.ts` (length → no tools + Steer; unknown → Steer; 401 fatal), `ai/features/llm-loop/llm-loop-reducer.test.ts` (unknown recoverable)                 |

### BUG-2026-07-25d — Empty `providerId` hides live catalog models

| Field                  | Value                                                                                                                                                                                    |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-25                                                                                                                                                                               |
| **Area**               | ui · config                                                                                                                                                                              |
| **Status**             | fixed                                                                                                                                                                                    |
| **Symptom**            | WS showed a full `langflower.models.catalog.snapshot`, but Inspector Model stayed empty; Provider looked selected.                                                                       |
| **Repro**              | Fresh skeleton workflow (`providerId: ""`) with one global provider → open LLM Inspector without changing Provider.                                                                      |
| **Root cause**         | HTML `<select>` with value `""` paints the first provider label while params stay `""`; Model resolved `catalogs[""]`. Same class as BUG-2026-07-25b, for unset rather than deleted ids. |
| **Fix**                | Blank `Select…` option for `""`; treat empty providerId as unset; auto-bind the sole configured provider; keep models catalog projection eagerly hot.                                    |
| **Design flaw signal** | Unset select values need an explicit blank option; never infer “selected provider” from the painted label.                                                                               |
| **Regression test**    | `packages/ui/src/app/features/canvas/tests/lf-inline-field.test.ts` (empty-string blank option)                                                                                          |

### BUG-2026-07-25c — Model catalog latched empty after failed live fetch

| Field                  | Value                                                                                                                                                                                 |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-25                                                                                                                                                                            |
| **Area**               | ui · ws · config                                                                                                                                                                      |
| **Status**             | fixed                                                                                                                                                                                 |
| **Symptom**            | Inspector Model stayed on muted “No models available” even after LM Studio / `baseURL` were correct; server `models.refresh` returned a full list when probed.                        |
| **Repro**              | First fetch empty (missing `/v1` or provider briefly down) → fix config → keep Inspector open without provider change: list never refreshes.                                          |
| **Root cause**         | Live catalogs were **client-authoritative** (`models.refresh.requested` + Inspector latch). Empty/wrong first answers left the UI owning retry policy instead of the server.          |
| **Fix**                | Server-pushed `langflower.models.catalog.snapshot` after config on connect/Save; UI folds only (`ModelsCatalogProjectionService`); removed client refresh intent and latch machinery. |
| **Design flaw signal** | Live option catalogs are server facts (like palette/config), not Inspector RPC. Client latch/retry is a smell that authority is on the wrong side of the bus.                         |
| **Regression test**    | `packages/server/src/bridge/push-models-catalog.test.ts`; `packages/ui/src/app/services/tests/models-catalog-projection.service.test.ts`                                              |

### BUG-2026-07-25b — Deleted provider leaves Inspector stuck (false “local” select)

| Field                  | Value                                                                                                                                                                                                   |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-25                                                                                                                                                                                              |
| **Area**               | ui · config                                                                                                                                                                                             |
| **Status**             | fixed                                                                                                                                                                                                   |
| **Symptom**            | After deleting a provider (e.g. `local2`), Helper Inspector still showed Model error `Provider "local2" is not configured` while Provider looked like `local`; changing Provider in the UI did nothing. |
| **Repro**              | Node `providerId=local2` → remove `local2` from langflower.jsonc → open Inspector: select shows first remaining provider label; params stay `local2`; Model disabled with missing-provider error.       |
| **Root cause**         | HTML `<select>` with no matching `<option>` displays the first option without updating the bound value; re-choosing that label often skips `change`.                                                    |
| **Fix**                | `withOrphanSelectOptions` in `lf-inline-field` — keep stale values visible as `… (missing)` so the user can pick a live catalog entry.                                                                  |
| **Design flaw signal** | Catalog-driven selects must surface orphaned stored values; never rely on the browser’s first-option display as the true selection.                                                                     |
| **Regression test**    | `packages/ui/src/app/features/canvas/tests/lf-inline-field.test.ts` (`withOrphanSelectOptions`)                                                                                                         |

### BUG-2026-07-25 — Inspector Model list empty until provider switch

| Field                  | Value                                                                                                                                                                                                 |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-25                                                                                                                                                                                            |
| **Area**               | ui · ws · config                                                                                                                                                                                      |
| **Status**             | fixed                                                                                                                                                                                                 |
| **Symptom**            | Agent Inspector Model select stayed empty on first open (especially `npx langflower` with providers only in global config and no static `models`); switching `providerId` away and back populated it. |
| **Repro**              | Global-only provider without `models: []`; open LLM node without changing provider → empty Model list; change provider and back → list appears.                                                       |
| **Root cause**         | Inspector latched `lastModelsRefreshProviderId` **before** a `models.snapshot` (and did not gate on WS `connected`). Early `TRANSPORT_NOT_OPEN` or a dropped intent left the latch set with no retry. |
| **Fix**                | Initially client connected-gate/latch; superseded by server-pushed `langflower.models.catalog.snapshot` (see BUG-2026-07-25c). Empty+failed catalog disables select with red error.                   |
| **Design flaw signal** | Fire-and-forget client refresh for live catalogs puts authority on the wrong side of the bus — prefer server-pushed snapshots.                                                                        |
| **Regression test**    | `packages/ui/src/app/features/sidebar/tests/models-field-presentation.test.ts`; `push-models-catalog.test.ts`                                                                                         |

### BUG-2026-07-24 — Custom pack typecheck fails without pack node_modules (rxjs .d.ts)

| Field                  | Value                                                                                                                                                                                                 |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-24                                                                                                                                                                                            |
| **Area**               | compiler · custom-nodes                                                                                                                                                                               |
| **Status**             | fixed                                                                                                                                                                                                 |
| **Symptom**            | Peer-only pack with valid `rxjs` / `@langflower/node-sdk` imports wrote `COMPILATION_ERRORS.md` (“Could not find a declaration file…”) even with no author deps.                                      |
| **Repro**              | Pack with `tsconfig.json` + `peerDependencies` only, no pack/`project` `node_modules`; Update / `compileProjectNodes`.                                                                                |
| **Root cause**         | Host paths mapped `import.meta.resolve('rxjs')` → `dist/cjs/index.js` and guessed sibling `.d.ts`; rxjs types live at package.json `types` / `exports.types`. Also `typeRoots` assumed monorepo root. |
| **Fix**                | `packages/compiler/src/resolve-host-types.ts` — resolve peers from compiler install via package.json types/exports; `@types/node` as compiler dep for host `typeRoots`.                               |
| **Design flaw signal** | Host type resolution must follow npm package metadata and the **Langflower install tree**, not project layout or JS↔DTS filename symmetry.                                                            |
| **Regression test**    | `packages/compiler/src/resolve-host-types.test.ts`; `compile-project-nodes.test.ts` — peer-only pack without `node_modules`.                                                                          |

### BUG-2026-07-23d — Copied workflow JSON appears twice / wrong id

| Field                  | Value                                                                                                                                                              |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Date**               | 2026-07-23                                                                                                                                                         |
| **Area**               | server · workflow · catalog                                                                                                                                        |
| **Status**             | fixed                                                                                                                                                              |
| **Symptom**            | Copy-pasting a workflow file (same `metadata.id`, new filename) listed the workflow twice or loaded the wrong file because catalog id came from embedded metadata. |
| **Repro**              | Duplicate `fake-llm.json` as `fake-llm-copy.json` without changing `metadata.id` → catalog shows colliding ids.                                                    |
| **Root cause**         | Identity lived inside JSON while the filesystem already keyed `{id}.json`.                                                                                         |
| **Fix**                | ADR-029: omit identity from disk JSON; bridge/session use sibling `workflowId` (= filename stem); no metadata hydrate.                                             |
| **Design flaw signal** | **Two sources of identity** (filename vs embedded id) — pick the filesystem key; never mirror it inside the file.                                                  |
| **Regression test**    | `packages/server/src/workflow/workflow.service.test.ts` — filename stem → `workflowId`; save omits identity from JSON                                              |

### BUG-2026-07-23c — Fake LLM stalls on load when persisted `inputs: {}`

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                     |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-23                                                                                                                                                                                                                                                                                                                                                |
| **Area**               | server · runtime · workflow · fake-llm                                                                                                                                                                                                                                                                                                                    |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                     |
| **Symptom**            | Demo `fake-llm` receives `userPrompt: "Hello"` then never emits reasoning/response (`runner` stays running). Recreating the Fake LLM node “fixed” it; `git restore` of the workflow + reload brought the stall back.                                                                                                                                      |
| **Repro**              | Load HEAD `demo-project/.../fake-llm.json` (`common-fake-llm` with `"inputs": {}`) → Run → WS: string/Hello → Fake LLM `userPrompt` value, no Fake LLM outputs.                                                                                                                                                                                           |
| **Root cause**         | (1) Load materialize only `connect`'d keys present in JSON — empty `inputs` left inventory ports (`tools`/`mcp`/…) inactive so `combineInputs` never emitted. (2) Recreate baked `defaultInputsFromDefinition` into JSON, masking the hole. (3) Dual-package `@rx-evo` made `isInactive` miss ESM inactive sentinels so `applyPortDefaults` also skipped. |
| **Fix**                | ADR-028: persist only visible overrides ≠ default; materialize injects _current_ definition defaults for missing keys; normalize strip on load/save; structural Symbol sentinel for inactive/loading in `runtime-runner`.                                                                                                                                 |
| **Design flaw signal** | **Persisted defaults ≠ live defaults** — baking `defaultValue` into workflow JSON couples upgrades to disk and hides runtime seed failures. Empty `inputs` must be a first-class, working contract.                                                                                                                                                       |
| **Regression test**    | `packages/server/src/workflow/empty-inputs-fake-llm.materialize.test.ts`; `workflow-persisted-inputs.test.ts`; `apply-editor-mutation.test.ts` (add omits default inputs)                                                                                                                                                                                 |

### BUG-2026-07-23 — Inspector panel params silently dropped (`editor.updateNodes []`)

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Date**               | 2026-07-23                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Area**               | server · workflow · editor · inspector · ws                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Symptom**            | Changing Inspector panel numbers (`maxIterations`, `maxFeedbackTurns`) sent `editor.updateNode.requested` with the new params, but the server replied `editor.updateNodes: []` — values never stuck / saved. Reproduced while the workflow was **not** started (idle).                                                                                                                                                                           |
| **Repro**              | Open adversarial Proposer (`common-openai-llm`) while idle → edit Tool-loop max iterations / Max feedback turns → WS: request carries params, delta is `[]`.                                                                                                                                                                                                                                                                                     |
| **Root cause**         | Panel `params` patches incorrectly forced a full `bindWorkflowToSessionEditor`. Any rebind failure returned silent `[]` (no reason). Rebind fails whenever `RuntimeEditor.locked` — including session-idle / editor-locked desync — because `removeNode` is ignored and `addNode` then returns false. Params are session-document / next-run ctx seeds and do not need rebind. `isGraphLocked()` alone was a secondary / incomplete explanation. |
| **Fix**                | Params-only patches update the session document without rebind (idle and while `runnerStatus === 'running'`). Position / ui / inputs stay blocked when session-locked; inputs still rebind when unlocked.                                                                                                                                                                                                                                        |
| **Design flaw signal** | Authoring params were tied to topology rebind + dual locks (`runnerStatus` vs `editor.locked`) with a silent empty delta — Inspector looked editable, server dropped the intent.                                                                                                                                                                                                                                                                 |
| **Regression test**    | `packages/server/src/workflow/apply-editor-mutation.test.ts` (idle params; idle+editor.locked; running params; inputs still blocked); `tests/integration/ws/editor.ws.test.ts` (params delta)                                                                                                                                                                                                                                                    |

### BUG-2026-07-22d — Server-bound chat factory forced Fake LLM into OpenAI resolve

| Field                  | Value                                                                                                                                                                    |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Date**               | 2026-07-22                                                                                                                                                               |
| **Area**               | server · common-nodes · fake-llm                                                                                                                                         |
| **Status**             | fixed                                                                                                                                                                    |
| **Symptom**            | WS `execute-fake-llm` timed out; runtime errors `Provider "fake" is not configured` while Fake should stream the imitate path.                                           |
| **Repro**              | Run `tests/integration/ws/execute-fake-llm.ws.test.ts` after LLM session-shell drain (server always injects `createChatCompletionStream`).                               |
| **Root cause**         | `buildExecutionContext` bound OpenAI `createChatCompletionStream` on every node; Fake treated any EC factory as “enable tool loop” and called `resolveProvider("fake")`. |
| **Fix**                | Omit `createChatCompletionStream` for `common-fake-llm`; prefer scripted factory over injected EC factory when `scriptedToolTurns` is set.                               |
| **Design flaw signal** | Optional test-injection field reused as always-on server bind without per-node product semantics — Fake’s default imitate path was overridden silently.                  |
| **Regression test**    | `packages/server/src/bridge/build-execution-context.test.ts` — omits factory for Fake; `tests/integration/ws/execute-fake-llm.ws.test.ts`                                |

### BUG-2026-07-22c — Editor ↔ session dual-write could drift topology

| Field                  | Value                                                                                                                                                                                        |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-22                                                                                                                                                                                   |
| **Area**               | server · workflow · editor                                                                                                                                                                   |
| **Status**             | fixed                                                                                                                                                                                        |
| **Symptom**            | Missed dual-write after an editor mutation could leave `session.activeWorkflow` node/edge ids out of sync with `RuntimeEditor` — save ≠ run / multi-tab desync.                              |
| **Repro**              | Code audit (`docs/code-regression/server-core.md` finding #4); unit path in `apply-editor-mutation.test.ts` (topology single-writer).                                                        |
| **Root cause**         | Separate helpers updated editor and session document independently (`upsertActiveWorkflowNode`, edge append/remove/replace).                                                                 |
| **Fix**                | Single-writer: mutate `RuntimeEditor`, then `syncActiveWorkflowTopologyFromEditor` rebuilds session topology from editor (payloads keyed by id). Load path still document → editor via bind. |
| **Design flaw signal** | Two live sources of truth for the same graph without a mandatory sync step — parallel updates can silently diverge.                                                                          |
| **Regression test**    | `packages/server/src/workflow/apply-editor-mutation.test.ts` — `editor ↔ session topology single-writer`                                                                                     |

### BUG-2026-07-22b — Bypass slot identity still multi-keyed after resume fix

| Field                  | Value                                                                                                                                                                                                                                                                                                      |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-22                                                                                                                                                                                                                                                                                                 |
| **Area**               | runtime · checkpoints · resume                                                                                                                                                                                                                                                                             |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                      |
| **Symptom**            | Residual of BUG-2026-07-20: edge `[base,index]`, checkpoint/`tap` `ch@n`, and SlotKey `base@index` were converted ad-hoc — easy to regress the next resume/telemetry path.                                                                                                                                 |
| **Repro**              | Code audit (`docs/code-regression/runtime.md` finding #2); unit round-trip in `bypass-ports.test.ts`. Resume contract stays in `resume.workflow.test.ts`.                                                                                                                                                  |
| **Root cause**         | Three encodings existed without a single conversion module; call sites could concat/`parse` `@` independently.                                                                                                                                                                                             |
| **Fix**                | Pure identity API in `packages/runtime/src/bypass-ports.ts` (`bypassOutputPortId` / `parseBypassOutputPortId` / `bypassSlotKey*` / `checkpointPortIdForSlot`); `wireScope` + `collectClusterSlotKeys` use it. UI `toSlotHandle` / `splitSlotHandle` re-export the same helpers from `@langflower/runtime`. |
| **Design flaw signal** | Parallel string encodings for one slot without a mandatory convert layer — same class as BUG-2026-07-20.                                                                                                                                                                                                   |
| **Regression test**    | `packages/runtime/src/bypass-ports.test.ts` — edge ↔ outputPortId ↔ SlotKey round-trip; existing resume/router tests                                                                                                                                                                                       |

### BUG-2026-07-22a — Palette catalog refresh wiped the live canvas model

| Field                  | Value                                                                                                                                                                                         |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-22                                                                                                                                                                                    |
| **Area**               | canvas · ui                                                                                                                                                                                   |
| **Status**             | fixed                                                                                                                                                                                         |
| **Symptom**            | Refreshing the node palette (new `palette.snapshot` reference) could reset the ng-diagram model — selection, live edits, and viewport hydrate could behave as if the graph was reloaded.      |
| **Repro**              | Open a workflow with nodes → trigger palette reload (catalog refresh) without changing the workflow graph → observe `modelAdapter` / `initializeModel` re-run.                                |
| **Root cause**         | `FlowCanvasComponent.modelAdapter` computed tracked both `graphInput()` and `palette()`, so any new palette input identity re-called `initializeModel`.                                       |
| **Fix**                | Track only `graphInput()` in `modelAdapter` (`untracked` palette for seed); subsequent palette inputs patch live `portsConfig` via `updateNodes`. Hydrate gate resets on graph identity only. |
| **Design flaw signal** | Treating a catalog/metadata stream as a full model seed dependency; seed vs live-patch ownership must be split when inputs share a host.                                                      |
| **Regression test**    | `packages/ui/src/app/features/canvas/tests/flow-canvas.component.test.ts` — `keeps modelAdapter identity when only palette changes`                                                           |

### BUG-2026-07-21f — Runner lifecycle unicast hid Run only on the starting tab

| Field                  | Value                                                                                                                                                                                              |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-21                                                                                                                                                                                         |
| **Area**               | ws · server · ui                                                                                                                                                                                   |
| **Status**             | fixed                                                                                                                                                                                              |
| **Symptom**            | Starting a run in tab A switched A to Stop; tab B kept showing Run. Telemetry (`output-emitted` / `done`) still appeared on B.                                                                     |
| **Repro**              | Open two editor tabs on the same server; click Run in one; peer tab Run/Stop chrome does not flip.                                                                                                 |
| **Root cause**         | `wire-runner-handlers` emitted `runner.started` / `startNode.started` / `interrupted` / `resume.started` via `clientEmit` (initiator only). UI `isRunning` already folds those facts from the bus. |
| **Fix**                | Broadcast those lifecycle facts with `bridgeEmit` (keep `runner.resume.failed` unicast).                                                                                                           |
| **Design flaw signal** | **Session-shared run gate must fan-out like editor deltas** — lifecycle facts that drive multi-tab chrome are not RPC replies to the clicking client.                                              |
| **Regression test**    | `tests/integration/ws/ws-session-sync.ws.test.ts` — broadcasts `runner.started` and `runner.interrupted` to peer clients                                                                           |

### BUG-2026-07-21e — Back-edge wires freeze after node drag

| Field                  | Value                                                                                                                                                                                                                                                 |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-21                                                                                                                                                                                                                                            |
| **Area**               | canvas · ui                                                                                                                                                                                                                                           |
| **Status**             | fixed                                                                                                                                                                                                                                                 |
| **Symptom**            | After dragging a node, wires (esp. feedback / below-routed back-edges) detach from ports or keep stale U-paths; reload fixes them.                                                                                                                    |
| **Repro**              | Wire a leftward loop (e.g. Review Gate.feedback → FakeLLM); drag either node; stubs stay at old coordinates until tab reload.                                                                                                                         |
| **Root cause**         | `LfEdgeChrome` passed `routingMode: 'manual'` + `points` into `ng-diagram-base-edge`. Base-edge `syncEdgePropertiesToModel` writes those into the model; auto re-route on drag is skipped, so `sourcePosition`/`targetPosition` and waypoints freeze. |
| **Fix**                | Below-route via `createBackEdgeAwareOrthogonalRouting` (replaces built-in `orthogonal` on `diagramInit`, stays auto). Chrome only dashes when bounds are missing. Clear any stuck `routingMode: 'manual'` on init.                                    |
| **Design flaw signal** | **Custom edge templates must not push `routingMode: 'manual'` into the bound edge** — ng-diagram treats that as model state, not a render override. Prefer `registerRouting` / auto `computePoints` for dynamic geometry.                             |
| **Regression test**    | `packages/ui/src/app/features/canvas/tests/compute-back-edge-aware-points.test.ts`; `lf-edge-chrome.test.ts` (isBack re-eval after position change)                                                                                                   |

### BUG-2026-07-21d — Review Gate dropped subscription to `result`

| Field                  | Value                                                                                                                                                                                                                                                                              |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-21                                                                                                                                                                                                                                                                         |
| **Area**               | hitl · execution · common-nodes                                                                                                                                                                                                                                                    |
| **Status**             | fixed                                                                                                                                                                                                                                                                              |
| **Symptom**            | Fake LLM streamed `reasoning` / `draftResponse`, but `response` never appeared in WS/runtime when wired only into Review Gate.`result` (demo `fake-llm.json`).                                                                                                                     |
| **Repro**              | Wire `Fake LLM.response → Review Gate.result` and `Review Gate.feedback → Fake LLM.feedback`. Run; wait for stream to finish. No `output-emitted` for `llm.response`. Unit: `emits response when Review Gate feedback is wired`.                                                   |
| **Root cause**         | Removing the `preview` passthrough left `result` consumed only via `withLatestFrom` on Approve. Wired upstream edges need a lasting subscriber on the target input/output chain; without `preview`, nothing pulled `result`, so the LLM `response` edge stayed cold.               |
| **Fix**                | Restore `configureOutput('preview', result)` and HITL `promptFrom: 'preview'` on approve / request-changes.                                                                                                                                                                        |
| **Design flaw signal** | **Passthrough / prompt outputs are demand drivers, not just UI.** Dropping an “unused” output can silently stop pulling upstream reactive edges. `withLatestFrom` alone does not create run-lifetime demand — now **forbidden** without human OK ([REACTIVITY.md](REACTIVITY.md)). |
| **Regression test**    | `packages/common-nodes/src/ai/nodes/fake-llm/node.test.ts` — `emits response when Review Gate feedback is wired`                                                                                                                                                                   |

### BUG-2026-07-21c — Fake LLM final response appeared missing

| Field                  | Value                                                                                                                                                                                                                                    |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-21                                                                                                                                                                                                                               |
| **Area**               | execution · common-nodes                                                                                                                                                                                                                 |
| **Status**             | fixed                                                                                                                                                                                                                                    |
| **Symptom**            | Fake LLM streamed `reasoning` and `draftResponse`, but downstream `response` appeared never to arrive in the default demo configuration.                                                                                                 |
| **Repro**              | Subscribe to `common-fake-llm.response` with its default `tokenDelayMs: 40`; the final value arrived after approximately 35 seconds.                                                                                                     |
| **Root cause**         | The fake provider delayed every whitespace token in two deliberately long text blocks. The terminal `response` is emitted only after both streams finish, so the 40 ms default accumulated into a long silent wait for downstream ports. |
| **Fix**                | Stream sentence-sized chunks instead of whitespace tokens. The visible stream remains paced at 40 ms, while the terminal response follows promptly.                                                                                      |
| **Design flaw signal** | **Per-token demo pacing is an end-to-end latency contract.** When terminal work is sequenced after a stream, token granularity controls downstream completion time, not merely UI animation.                                             |
| **Regression test**    | `packages/common-nodes/src/ai/nodes/fake-llm/node.test.ts` — standard Fake LLM stream test now runs with the default delay and requires `response`.                                                                                      |

### BUG-2026-07-21b — HITL replies missing from feed after tab reload

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                       |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-21                                                                                                                                                                                                                                                                                                                                                  |
| **Area**               | ui · hitl · feed · ws                                                                                                                                                                                                                                                                                                                                       |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                       |
| **Symptom**            | During a live run, Review Gate / Ask User replies appear in the work log. After browser tab reload, sections return but nested HITL answer bubbles are gone.                                                                                                                                                                                                |
| **Repro**              | Run a HITL loop → submit feedback → reload tab while the session still has an `executionFeed`. Live path OK; reconnect path empty `feedUserTurns`. Unit: feed-before-palette sequence in `workflow-execution.service.test.ts`.                                                                                                                              |
| **Root cause**         | Bootstrap emits `executionFeed.snapshot` before `workflow.current.snapshot` / `palette.snapshot`. Feed fold used `withLatestFrom` on lookup maps that `startWith(empty)` — replay ran with no defs, skipped HITL `input-received`, and never re-applied when palette arrived. Awaiting-HITL fold already used `combineLatest` (tabs OK); feed fold did not. |
| **Fix**                | Gate feed snapshot actions on real workflow + palette Subjects via `combineLatest`, apply once per feed identity (`distinctUntilChanged` on the feed reference) so later catalog updates do not wipe live turns.                                                                                                                                            |
| **Design flaw signal** | **False-ready context** — `startWith(empty)` + `withLatestFrom` makes a one-shot snapshot hydrate look successful while classifying events with empty defs. Prefer `combineLatest` on raw catalog Subjects (or re-hydrate when defs arrive with a `live` gate). See [REACTIVITY.md](REACTIVITY.md) § False-ready context.                                   |
| **Regression test**    | `packages/ui/src/app/services/tests/workflow-execution.service.test.ts` — `replays HITL replies when feed arrives before workflow and palette`; palette-update / later-snapshot guards                                                                                                                                                                      |

### BUG-2026-07-21 — Live settle wiped canvas chrome that reconnect snapshot restored

| Field                  | Value                                                                                                                                                                                                                                                      |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-21                                                                                                                                                                                                                                                 |
| **Area**               | execution · ui · detachable-long-run                                                                                                                                                                                                                       |
| **Status**             | fixed                                                                                                                                                                                                                                                      |
| **Symptom**            | After `runner.done`, node/edge chrome cleared to idle while the feed still showed the settled run. Reopen/reconnect restored chrome from `executionFeed.snapshot` — live settle and reconnect disagreed (detachable-long-run S2–S3).                       |
| **Repro**              | Run a linear string→finish graph; watch chrome clear on done. Reload: chrome returns from snapshot replay. Unit: former `clears on run done` chrome tests.                                                                                                 |
| **Root cause**         | `WorkflowExecutionService` cleared node/edge chrome maps on `runner.done` / `runner.interrupted`, while reconnect rebuilt the same maps from the feed log. Live and reconnect paths projected different settled visuals from the same events.              |
| **Fix**                | Stop clearing chrome on done/interrupt; keep final port states. New runs still reset via `runner.started`; null/idle snapshots still replace. Derive settle progress (`completed` / `failed` / `completed_with_errors`) from the event log for feed + CLI. |
| **Design flaw signal** | **Settle must not fork live vs reconnect projections** — if reconnect replays the feed into chrome, live settle must leave the same chrome, not wipe it on a lifecycle side channel.                                                                       |
| **Regression test**    | `workflow-execution.service.test.ts` (keeps chrome after done; mid-run / settled / null snapshot gate); `lf-node-chrome` / `lf-edge-chrome` settled chrome; `tests/integration/ws/detachable-long-run.ws.test.ts`                                          |

### BUG-2026-07-20 — Palette drag ghost is viewport-wide

| Field                  | Value                                                                                                                                                                                                         |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-20                                                                                                                                                                                                    |
| **Area**               | ui · palette                                                                                                                                                                                                  |
| **Status**             | fixed                                                                                                                                                                                                         |
| **Symptom**            | Dragging a node from the palette shows a ghost with a huge empty width                                                                                                                                        |
| **Repro**              | Open editor → drag any palette node onto the canvas; observe drag image width                                                                                                                                 |
| **Root cause**         | `attachPaletteDragImage` mounts a `position: fixed` block host on `document.body`. Block + fixed sizes to the viewport; `dataTransfer.setDragImage(host)` captures that full box, not the compact node chrome |
| **Fix**                | Host `display: inline-block; width: max-content` in `palette-drag-image.ts`; preview host class `inline-block w-max`                                                                                          |
| **Design flaw signal** | Off-screen drag/measure hosts inherit block layout defaults; sizing for `setDragImage` must be explicit shrink-to-content                                                                                     |
| **Regression test**    | `packages/ui/src/app/features/palette/tests/palette-drag-image.test.ts`                                                                                                                                       |

### BUG-2026-07-20 — Resume replays all router bypass slots from base port `ch`

| Field                  | Value                                                                                                                                                                                                                                                         |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-20                                                                                                                                                                                                                                                    |
| **Area**               | runtime · checkpoints · resume                                                                                                                                                                                                                                |
| **Status**             | fixed                                                                                                                                                                                                                                                         |
| **Symptom**            | Stop mid-`common-delay` on Router→Delay graph → Continue finishes immediately instead of waiting remaining/full delay                                                                                                                                         |
| **Repro**              | Demo `example.json` (String+Number→Router→Delay→Preview→Finish): Run → Stop during Delay → Continue. Unit: `packages/runtime/src/resume.workflow.test.ts` (`replays distinct router bypass slot snapshots…`)                                                  |
| **Root cause**         | Checkpoint stores bypass slots as `ch` / `ch@1` / …; resume `watchedByKey` for edge `slotKey(router, 'ch', n)` looks up `outputSnapshots.get('ch')` for every slot, so Delay.`delay` gets the string from `ch` and RxJS `delay(non-number)` collapses to ~1ms |
| **Fix**                | `bypassOutputPortId` + resume snapshot/telemetry lookup by `ch` / `ch@n` in `runtime-runner.wireScope`; identity API later centralized (see BUG-2026-07-22b)                                                                                                  |
| **Design flaw signal** | Bypass slot identity split three ways (edge `[base, index]`, outputs map `ch@n`, checkpoint key `ch@n`) without a single resume keying rule                                                                                                                   |
| **Regression test**    | `packages/runtime/src/resume.workflow.test.ts` — bypass Delay + merge/combine multi-input resume cases                                                                                                                                                        |

### BUG-2026-07-19d — Integration temp cleanup fails with ENOTEMPTY on Windows

| Field                  | Value                                                                                                                                                         |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-19                                                                                                                                                    |
| **Area**               | testing · ws                                                                                                                                                  |
| **Status**             | fixed                                                                                                                                                         |
| **Symptom**            | Full `verify` fails 11 WS integration suites with `ENOTEMPTY` on `rmdir` of `tests/tmp/...` after tests already passed                                        |
| **Repro**              | `node build/tools/agent-run.mjs verify` on Windows after checkpoint-heavy WS suites                                                                           |
| **Root cause**         | `removeTempProject` used single-shot `fs.rm`; Windows still holds handles briefly after `server.close`. Node's `fs.rm` `{ maxRetries }` is ignored on Windows |
| **Fix**                | Retry loop with backoff in `tests/integration/helpers/temp-project.ts` for `ENOTEMPTY` / `EBUSY` / `EPERM` / `EACCES`                                         |
| **Design flaw signal** | Teardown assumed POSIX-like immediate unlink after close; durable run dirs (checkpoints) increase Windows handle lifetime                                     |
| **Regression test**    | none — harness flake; covered by re-running integration suite                                                                                                 |

### BUG-2026-07-19c — Role preset `{md,mdx}` globs never matched

| Field                  | Value                                                                                                      |
| ---------------------- | ---------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-19                                                                                                 |
| **Area**               | tools · permissions · llm-presets                                                                          |
| **Status**             | fixed                                                                                                      |
| **Symptom**            | Plan/Explorer `write`/`create` of `*.md` outside `docs/**` was deny-closed with no `permission.ask`        |
| **Repro**              | Plan preset + harness `write` `plans/fix.md` in basic-coder pilot CI                                       |
| **Root cause**         | Preset used `**/*.{md,mdx}`; `matchPermissionPattern` has no brace expansion, so only `*` (`deny`) matched |
| **Fix**                | Split into `**/*.md` + `**/*.mdx` in `llm-role-preset.ts`; docs aligned                                    |
| **Design flaw signal** | Documented “OpenCode-style” globs implied bash braces without implementing them in the matcher             |
| **Regression test**    | `tests/integration/ws/execute-basic-coder.ws.test.ts` (Plan write `plans/fix.md` emits `permission.ask`)   |

### BUG-2026-07-19b — Tool allowlist filtered inventory only, not invoke

| Field                  | Value                                                                                                                                                                            |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-19                                                                                                                                                                       |
| **Area**               | execution · common-nodes                                                                                                                                                         |
| **Status**             | fixed                                                                                                                                                                            |
| **Symptom**            | `enabledToolIds` hid tools from the model inventory, but a hallucinated `tool_calls` name could still invoke any builtin via `ctx.harness`.                                      |
| **Repro**              | Internal loop with inventory `['read']`; completion returns `write` tool call → harness wrote the file.                                                                          |
| **Root cause**         | Allowlist applied only in `mergeToolInventory`; `runInternalToolLoop` invoked harness without checking the filtered tools list.                                                  |
| **Fix**                | Reject non-allowlisted tool ids before invoke in `run-internal-tool-loop.ts` (handler path or `harness.invoke`).                                                                 |
| **Design flaw signal** | **Inventory filtering ≠ execution gate.** Author-time allowlists must be enforced at invoke, not only at prompt/tool-definition assembly.                                        |
| **Regression test**    | `packages/common-nodes/src/ai/run-internal-tool-loop.test.ts`                                                                                                                    |
| **Follow-up**          | Domain/custom tools no longer use a harness `toolId` map — attach `registration.handler` ([ADR-019](ADR.md#adr-019--tool-handlers-on-registration-not-harness-toolid-registry)). |

### BUG-2026-07-19 — Soft↔Hard debate loop never materializes (wired feedback)

| Field                  | Value                                                                                                                                                                                                |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-19                                                                                                                                                                                           |
| **Area**               | execution · common-nodes                                                                                                                                                                             |
| **Status**             | fixed                                                                                                                                                                                                |
| **Symptom**            | Soft↔Hard debate (`soft-vs-hard-harness` / fake-llm debate-loop) starts `running` but Soft never emits `response` / reasoning; UI looks stuck.                                                       |
| **Repro**              | Topic → Soft.userPrompt; Soft.response → Hard.userPrompt; Hard.response → Soft.feedback; no finish. Run with `common-fake-llm` (`tokenDelayMs: 0`).                                                  |
| **Root cause**         | Soft `combineInputs` included `feedback`. Wiring Hard→Soft.feedback marked the slot wired, so `applyPortDefaults` skipped `defaultValue: ''`. Soft waited for Hard; Hard waited for Soft.            |
| **Fix**                | ADR-016: init combine without feedback; feedback turn stream with `startWith` outside init (`openai-llm` + minimal split on `fake-llm`).                                                             |
| **Design flaw signal** | **`defaultValue` is not a cycle primer when the port is wired.** Loop-back turns need an explicit turn stream, not inventory-port defaults.                                                          |
| **Regression test**    | `packages/common-nodes/src/ai/nodes/fake-llm/debate-loop.node.test.ts`; `tests/integration/ws/execute-fake-llm-debate-loop.ws.test.ts`; openai multi-turn/init-recreate in `openai-llm/node.test.ts` |

### BUG-2026-07-17d — delete active workflow crashed UI on null graph

| Field                  | Value                                                                                                                                                              |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Date**               | 2026-07-17                                                                                                                                                         |
| **Area**               | ui · shared · ws                                                                                                                                                   |
| **Status**             | fixed                                                                                                                                                              |
| **Symptom**            | Deleting the active workflow left the editor broken: `TypeError: Cannot read properties of null (reading 'graph')` in `workflow-execution.service.ts`.             |
| **Repro**              | Load a workflow → Delete → confirm.                                                                                                                                |
| **Root cause**         | Server correctly emits `workflow.current.snapshot` with `activeWorkflow: null`, but shared typed it as non-null and UI folds/canvas read `.graph` unconditionally. |
| **Fix**                | `WorkflowCurrentSnapshotPayload.activeWorkflow` is `                                                                                                               | null`; null-safe node maps in `WorkflowExecutionService`; canvas container skips `lf-flow-canvas` when no active workflow. |
| **Design flaw signal** | **Nullable session facts must be typed and handled at every snapshot consumer.** A successful delete is a first-class empty state, not an edge the UI can ignore.  |
| **Regression test**    | `packages/ui/src/app/services/tests/workflow-execution.service.test.ts` (`tolerates null activeWorkflow after delete without throwing`)                            |

### BUG-2026-07-17c — parallel HITL tab lingered after `runner.done`

| Field                  | Value                                                                                                                                                                                                     |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-17                                                                                                                                                                                                |
| **Area**               | ui · hitl                                                                                                                                                                                                 |
| **Status**             | fixed                                                                                                                                                                                                     |
| **Symptom**            | Two Review Gates awaited; approving one finished the run (`runner.done`); the second composer tab stayed visible with no live runner behind it.                                                           |
| **Repro**              | Parallel HITL gates → reply on one branch that reaches finish → observe leftover tab for the unanswered gate.                                                                                             |
| **Root cause**         | HITL awaiting fold hard-reset only on `interrupted` / new `runId`, deliberately skipping `runner.done` (to avoid nuclear wipes via `!isRunning` hydrate). After a true settle, orphan tabs are dead UI.   |
| **Fix**                | Hard-reset awaiting on `runner.done` as well as interrupt / new `runId`. Keep hydrate ungated on `!isRunning`.                                                                                            |
| **Design flaw signal** | **Settled run ≠ mid-flight status flip.** Protecting parallel siblings from hydrate/`isRunning` gates is right; leaving composer open after `runner.done` is not — `done` is an explicit end-of-run fact. |
| **Regression test**    | `packages/ui/src/app/services/tests/workflow-execution.service.test.ts` (`clears awaiting tabs on runner.done`)                                                                                           |

### BUG-2026-07-17 — Clear during an active run wiped feed chrome / looked like Stop

| Field                  | Value                                                                                                                                                                                                                        |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-17                                                                                                                                                                                                                   |
| **Area**               | server · ui · hitl                                                                                                                                                                                                           |
| **Status**             | fixed                                                                                                                                                                                                                        |
| **Symptom**            | Pressing Clear during an active run wiped the work log **and** canvas node/wire highlights; earlier also made the UI treat the run as stopped when `runId` was nulled.                                                       |
| **Repro**              | Start a workflow (or HITL), wait for canvas chrome / Review Gate, click Clear in the work-log header.                                                                                                                        |
| **Root cause**         | Clear shared `executionFeed.snapshot` with the chrome folds — an empty (or null) snapshot replayed as a full chrome reset. Nulling `runId` mid-run also collapsed the UI run-gate.                                           |
| **Fix**                | Clear is forbidden while `runnerStatus === 'running'`: UI hides the button (`!isRunning()`); server ignores `executionFeed.clear.requested`. Idle clear still wipes the log and drops `runId`.                               |
| **Design flaw signal** | **Feed-clear is not a mid-run operation.** The same snapshot channel feeds timeline and canvas chrome; clearing history during a live run has no safe shared meaning — gate the intent instead of special-casing every fold. |
| **Regression test**    | `packages/server/src/session/clear-feed-while-running.test.ts`                                                                                                                                                               |

### BUG-2026-07-17b — remounted Run/Stop stayed disabled (no replayed workflow snapshot)

| Field                  | Value                                                                                                                                                                                 |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-17                                                                                                                                                                            |
| **Area**               | ui                                                                                                                                                                                    |
| **Status**             | fixed                                                                                                                                                                                 |
| **Symptom**            | Stop next to the HITL tab/label was always disabled / greyed out.                                                                                                                     |
| **Repro**              | Start a HITL run so the composer chrome mounts a compact `lf-run-button` (footer button destroyed).                                                                                   |
| **Root cause**         | `isDisabled` read `activeWorkflow` from `workflow.current.snapshot.pipe(startWith(null))`. Subjects do not replay; a freshly mounted button stayed on `null` until the next snapshot. |
| **Fix**                | Gate Run on singleton `WorkflowExecutionService.hasRunnableGraph()` (`shareReplay` workflow fold); never disable while `isRunning()`.                                                 |
| **Design flaw signal** | **Do not derive enablement from a non-replaying bus Subject inside a component that remounts.** Prefer a root service projection with `shareReplay`, or keep the control mounted.     |
| **Regression test**    | `packages/ui/src/app/services/tests/workflow-execution.service.test.ts` (`hasRunnableGraph`)                                                                                          |

### BUG-2026-07-16 — subscribing `WorkflowExecutionService` to `runner.snapshot` triggers NG0200 in standalone component tests

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Date**               | 2026-07-16                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Area**               | ui · hitl · ws                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Status**             | fixed (derive `isRunning` from run-lifecycle events instead)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Symptom**            | Full vitest `--unit` suite failed with `RuntimeError: NG0200: Circular dependency detected for WorkflowExecutionService` in every `TestBed` that declares a component injecting `WorkflowExecutionService` (`dynamic-port-update`, `lf-node-chrome`, `lf-edge-chrome`, `workflow-execution.service.test`). The `ngTokenPath` listed only `[WorkflowExecutionService]` — no second token — so the cycle was invisible from the DI graph.                                                                                                                            |
| **Repro**              | Add `this.bridge.raw['runner.snapshot'].subscribe(...)` inside the `WorkflowExecutionService` constructor (alongside the existing `runner.started`/`runner.done`/… subscriptions), build, run the canvas unit tests → NG0200. Remove that single subscription → all green. Confirmed by bisection: reverting the whole service file fixed it; re-adding every change except the `runner.snapshot` subscription kept it green; re-adding only the `runner.snapshot` subscription reproduced it.                                                                     |
| **Root cause**         | Angular JIT (vitest) computes injector scope for `providedIn: 'root'` services from the **module import graph**, not the constructor parameter list. `runner.snapshot` is the only _snapshot-style_ event key the service touched, and its payload type (`RunnerSnapshotPayload` → `@langflower/runtime` types) was the first time that particular runtime type was pulled into the service's load-order during those tests, surfacing a latent circular module-eval path through `@langflower/runtime` ↔ bridge config that the event-style keys did not trigger. |
| **Fix**                | `isRunning` is now derived purely from the run-lifecycle events (`runner.started` / `runner.startNode.started` → `true`, `runner.done` / `runner.interrupted` → `false`); the `runner.snapshot` subscription was removed from the constructor. Mid-flight reconnect (a run already `running` at connect time) is no longer reflected into `isRunning`, but the runtime re-emits `runner.started`/`runner.startNode.started` on connect for the in-flight run, so the gate still settles correctly.                                                                 |
| **Design flaw signal** | **Avoid eager `subscribe()` to snapshot-style bus keys inside a `providedIn: 'root'` service constructor when the same fact is reachable from event-style keys.** Snapshot keys pull a different type-load path that can surface latent JIT circular-import cycles invisible from DI params. Prefer deriving UI gates from the existing event stream; extend the protocol/projection instead of adding a new eager subscription that duplicates a fact already on the wire.                                                                                        |
| **Regression test**    | `packages/ui/src/app/services/tests/workflow-execution.service.test.ts` (stub now includes `runner.snapshot` + `runner.input-received`); the full `--unit` suite stays NG0200-free after the fix.                                                                                                                                                                                                                                                                                                                                                                  |

### BUG-2026-07-15c — `@rx-evo` has no public merge-of-`raw$` primitive; `statefulObservable({ input })` re-subscribes a stream on every emission

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-15                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Area**               | execution · runtime                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Status**             | fixed (v1: values only); full `raw$` blocked on `@rx-evo` (documented on `PortMeta.mode: 'merge'`, 2026-07-22)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Symptom**            | `multi: 'merge'` input ports were collapsed with `'combine'` into one code path that always produced a `combineLatest` **array**, so `common-merge` forwarded `[x]` instead of `x`. When implementing a true flatten, the obvious `statefulObservable(merge(...sources.value$))` (passing the merged stream as `input`) produced a `StatefulObservable` whose `value$` never terminated (5000 ms test timeout) — every emission of the input stream re-triggered `@rx-evo`'s `combineLatest([source$, cache$]) → switchMap(loader)` machinery, re-subscribing the stream.                                                                                                                                                                                                   |
| **Repro**              | In `runtime-runner.ts` `multyInputGroups` post-loop, build the merge source as `statefulObservable(merge(...sources.map((s) => s.value$)))` and read its `value$` with `toArray()` → hangs. Build it as `statefulObservable({ loader: () => merge(...sources.value$) })` instead → terminates with the flattened values.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Root cause**         | `@rx-evo/stateful-observable` treats `input` as a **single** value (a param / seed), not a stream: its constructor does `combineLatest([source$, cache$]).pipe(switchMap(loader))` and re-runs the loader on each `source$` emission. Passing a multi-emission `Observable` as `input` makes it treat each element as a fresh seed and re-subscribe. The only public fan-in combinator is `combineStatefulObservables` (combineLatest → array). A `merge`-of-`raw$` combinator does not exist, and the `raw$`→`StatefulObservable` wrapper (`fillStatefulObservable`) is **internal/unexported**, so there is no public way to build a `StatefulObservable` from an arbitrary `raw$` stream (which is what full state forwarding — loading/error/inactive — would require). |
| **Fix**                | `multyInputGroups` now branches on `inputMeta.mode`: `'combine'` keeps `combineStatefulObservables(sources, v => v)` (array); `'merge'` flattens success values via `statefulObservable({ loader: () => merge(...sources.value$) })`. The `common-merge` node is unchanged (passthrough of the merged connection). `join-node` test harness switched from `mode: 'merge'` to `mode: 'combine'` (array-join is inherently combineLatest). New `merge-node` harness + test assert flatten (`['foo','bar']`, not `['foo']`/`[['foo'],['bar']]`). Full `raw$` passthrough waits on `@rx-evo`; limitation documented in `packages/runtime/src/types.ts` § Multi-input ports.                                                                                                     |
| **Design flaw signal** | **Streaming fan-in (merge) is not expressible with the public reactive API.** A `StatefulObservable` is entirely defined by its `raw$`, yet the library only lets you construct one from a `loader`/single-`input` — and `input` is semantically a value, not a stream. Any node that must flatten multiple reactive inputs, or forward non-success states, hits an API gap and must either misuse `input` (re-subscription bug) or reimplement the unexported `fillStatefulObservable`. Candidate ADR / principle: `@rx-evo` should expose a `mergeStatefulObservables(raw$)` (or export `fillStatefulObservable`) so runtime fan-in can forward full `ResponseWithStatus` state without duplication.                                                                      |
| **Regression test**    | `packages/runtime/src/testing/nodes/merge-node.test.ts` — merge flattens multi-input values (one emission per source, not an array) and rejects duplicate edges; `join-node.test.ts` still asserts combineLatest array join.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

### BUG-2026-07-15 — bypass ports (Router) never emit `output-emitted`, so downstream wires never highlight

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-15                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Area**               | execution · runtime · ui                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Symptom**            | Wire from a Router (bypass-only) to a Preview/sink node never shows the "working" (pending / dark-yellow) highlight while the run is in flight; it stays `idle`. Same for any edge whose source is a bypass output.                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Repro**              | Build `constant → router(ch) → preview`. Run and inspect the `runner.output-emitted` stream: the router's `ch` output emits nothing — only the downstream sink emits. `WorkflowExecutionService.edgeStates` never receives an `edgeId` for the router→sink edge, so `wireStatus` stays `'idle'`.                                                                                                                                                                                                                                                                                                                          |
| **Root cause**         | In `runtime-runner.ts` `runScope`, the source for a bypass edge was resolved via `getBypassConnection(...)` **raw**, while the telemetry tap (`watchedByKey`) wrapping the same bypass output was built but never wired into the dataflow. Bypass ports were deliberately connected raw "to avoid StatefulObservable loading semantics on a pure forward" — but that also suppressed all `output-emitted` telemetry, so `edgeIds` for bypass-sourced edges were never pushed. The pre-pass also hardcoded `slotIndex = 0` when building the tap, so multi-slot bypass ports wrapped the wrong slot connection.            |
| **Fix**                | `runScope` now prefers the `watchedByKey` tapped wrapper for bypass sources (same path as regular ports), so `output-emitted` rides the dataflow and fires as part of delivery; it falls back to the raw connection only when no tap was built for that slot. The tap build loop now uses the slot's actual `slotIndex` (via `parseSlotKey`) instead of a hardcoded `0`, so per-slot bypass telemetry is correct. Each `output-emitted` now carries `edgeIds` for every outgoing wire of the bypass port. The UI already renders whatever `edgeId` states it receives, so the wire highlights without further UI changes. |
| **Design flaw signal** | **Telemetry coupled to the connection strategy, not the dataflow.** "Pure forward" (raw bypass connect) was treated as mutually exclusive with telemetry, so choosing raw silently dropped observability. Telemetry should ride the dataflow path (a `pipe(tap)` that mirrors the source's loading) regardless of whether the port is bypass or regular — wrapping with `pipe(tap).with({ meta })` does not inject new loading semantics, so the bypass-vs-regular distinction was a false dichotomy for observability.                                                                                                   |
| **Regression test**    | `packages/runtime/src/testing/workflows/router.workflow.test.ts` — `emits output-emitted telemetry with edgeIds for bypass outputs (BUG CASE 1)`: the router's `output-emitted` events exist and each carries the outgoing edge ids.                                                                                                                                                                                                                                                                                                                                                                                      |

### BUG-2026-07-15b — execution chrome missing after a mid-run browser reload (direct-DOM reflection races the async canvas paint)

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-15                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Area**               | execution · ui · canvas                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Symptom**            | With a run in flight, reload the browser tab (`session.state.snapshot` replays `executionFeed`). After reconnect the work-log feed shows the run correctly, but the canvas wires/nodes are **not** highlighted — they stay `idle` even though `WorkflowExecutionService` has replayed the output states into its signals. (BUG CASE 2.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Repro**              | Open the editor, run `constant → router → delay(1000) → preview`. While the delay is pending, reload the tab. The canvas shows no working/pending chrome on the wires; the feed does. The same chrome worked fine on a fresh run (no reload), because then the events arrived after ng-diagram had already painted the elements.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Root cause**         | `FlowCanvasComponent.applyExecutionChrome()` reflected the shared execution state onto the canvas DOM by a **direct-DOM query** (`host.querySelectorAll('.lf-node-chrome[data-node-id]')` / `ng-diagram-edge[data-edge-id]`) driven from the bridge event subscriptions. It relied on the elements already existing in the DOM at the moment the event fired. On reconnect the snapshot event arrives **before** ng-diagram paints the nodes/edges, so the query found nothing and the chrome was never applied; nothing re-ran the reflection afterwards, so it stayed blank. This is a second instance of the same signal-as-DOM-load problem as BUG CASE 1: observability (here, the chrome paint) was coupled to a side-channel (event timing) instead of the actual render/state. The required pulse (a transient green flash) was also driven through a separate `ExecutionAnimationService` that emitted `highlight`/`clear` intents the canvas applied by yet another DOM query — the same race, plus an extra service. |
| **Fix**                | Removed `applyExecutionChrome` and `ExecutionAnimationService` entirely. Chrome is now **signals-native and self-contained per element**: `LfNodeComponent` reads `execution.nodeStatus(id)` (a `computed`) and binds `lf-node-chrome--pending/--value/--error`; a new `LfEdgeChromeComponent` (`NgDiagramEdgeTemplate`) reads `execution.wireStatus(id)` and binds `lf-edge--pending/--value/--error`. Because each element binds its own chrome from the reduced signal on its **first** render, the colour appears as soon as ng-diagram paints the element — no canvas-wide DOM sweep, no timing dependency. The transient pulse moved into each component, driven by a per-element filtered stream (`WorkflowExecutionService.getEventsForNode(id)` / `getEventsForEdge(id)` over `runner.output-emitted`), so it fires only on a live `value` and never re-fires on replay. Edges now carry `type: 'lf-edge'` so ng-diagram uses the custom template.                                                                     |
| **Design flaw signal** | **Reduce state once, project it per element — never re-derive it from a global side effect against a possibly-empty DOM.** A canvas-wide "reflect signals into the DOM by querySelector" routine is inherently racy against asynchronously-painted elements (and against reconnect, where the state event pre-dates the paint). Put the projection where the element is created (the component's template binding / `computed`), so it is correct on first paint and stays correct under replay. The same lesson applies to the pulse: drive transient flashes from a per-element event slice, not a separate service that re-introduces the same DOM-query race.                                                                                                                                                                                                                                                                                                                                                               |
| **Regression test**    | `packages/ui/src/app/features/canvas/tests/lf-node-chrome.test.ts` (asserts `lf-node-chrome--pending/--value/--error` and the `--pulse` flash render from the reduced signal) and `packages/ui/src/app/features/canvas/tests/lf-edge-chrome.test.ts` (asserts `lf-edge--pending` / `--value` and `--pulse`); the shared service's snapshot→signal replay is covered by `workflow-execution.service.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

### BUG-2026-07-14 — WS bridge drops `pending` events for the live client (server subscribes to `runner.events$` after run start)

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-14                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Area**               | execution · runtime · server · ui                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Symptom**            | A client already connected when the run starts sees no yellow "pending" indicator on reactive nodes such as `common-delay`. The work log / canvas chrome go straight from `inactive` to `value` on that live client; the `pending` pulse the UI uses for `nodeStatus === 'pending'` is never delivered over the WS bridge on the live path. Clients that connect _after_ the run has started do see the pulse (they replay the `executionFeed` snapshot), which masked the defect. The nodes themselves emit `pending` correctly — proven by `forward-runner-event.test.ts`.                                                                                                                                                                                     |
| **Repro**              | Connect a WS client, wire `constant → common-delay(1000) → finish`, run, and watch `runner.output-emitted` for the delay node: the live client receives only `state: 'value'` (no `pending`). A client connecting after the run already started receives `pending` then `value` (snapshot replay). Compare with `packages/server/src/bridge/forward-runner-event.test.ts` which exercises the bridge subscription directly.                                                                                                                                                                                                                                                                                                                                      |
| **Root cause**         | The reported bug (pending never delivered over the WS bridge on the live path) was a **server-side subscription race**: `attach-langflower-bridge.ts` subscribed to `runner.events$` only _per client_ at connect time, _after_ `runner.start()` had already fired the initial `pending` events into that `events$` — a plain, non-replaying RxJS `Subject`. Those emissions were dropped on the live path; clients connecting afterwards masked the loss by replaying the `executionFeed` snapshot. The library, runtime, and node definitions (including `common-delay`) emit `pending` correctly — proven by `pending-state.workflow.test.ts` and `forward-runner-event.test.ts`; the defect was purely the bridge subscription timing, not the node pattern. |
| **Fix**                | Server: `attach-langflower-bridge.ts` now holds a single always-on subscription to `session.runtime.runner.events$` (independent of clients/runs), fanning out to all connected clients via the existing `clientIndex`; re-subscription only if the runner instance changes (it is a stable singleton across runs and workflow loads — `loadWorkflowIntoSession` rebinds the same editor instead of recreating the runner). No replay buffer; late clients still receive the backlog through the existing `executionFeed` snapshot. No node-definition change was needed — the nodes already emit `pending`.                                                                                                                                                     |
| **Design flaw signal** | **Event-stream subscription timing vs. source lifecycle.** A plain (non-replaying) RxJS `Subject` used as the run's telemetry bus drops every emission that occurs before a subscriber attaches. The telemetry subscription must be established _before_ the run starts (or be always-on) — otherwise initial events are silently lost even when the source emits them. The node/definition layer was not at fault; the loss was entirely on the subscription-boundary side.                                                                                                                                                                                                                                                                                     |
| **Regression test**    | `tests/integration/ws/pending-events-bridge.ws.test.ts` — `single always-on subscription fans pending out to all connected clients` (the server fix) and `delay node emits pending then value through bridge` (over the real WS bridge). `packages/runtime/src/testing/workflows/pending-state.workflow.test.ts` — runtime emits `pending` for every node type in the harness. `packages/server/src/bridge/forward-runner-event.test.ts` — `pending` `output-emitted` events reach the bridge client.                                                                                                                                                                                                                                                            |

### BUG-2026-07-12c — work-log feed shows a node's output before its input (`output-emitted` precedes `input-received`)

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-12                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Area**               | execution · runtime · feed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Symptom**            | Two ordering symptoms in the work-log feed, both from the same cause: (1) per node, `output-emitted` appeared **before** `input-received`; (2) in the `string → delay(1000) → preview → finish` example the delayed node's **section** landed **last** (`finish → preview → delay`) — the delayed value reached the feed after `done`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Repro**              | Run any workflow with a chain (string → router → delay → preview → finish). Inspect the `runner.*` event stream on the WS bridge / sidebar feed; check both per-node input→output order and cross-node section order.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Root cause**         | In `runtime-runner.ts` `wireScope`, telemetry was produced by **two separate `.subscribe()` watchers** — `watchOutputPort` (subscribed to every output) and `watchInputSlot` (subscribed to every input) — in addition to the dataflow `connect()` graph. `@rx-evo` `StatefulObservable` notifies subscribers **FIFO**, and a subscribe is a (lazy) first subscriber that triggers value delivery. (1) Outputs were subscribed before inputs, so an output's telemetry fired before its input's. (2) Because inputs were watched _after_ outputs, a delayed node's downstream input received its value before the delayed source's own `output-emitted` fired — so `Delay` landed last. Data flow and event order were coupled through subscription order.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Fix**                | Removed the separate watchers entirely. Telemetry is now produced **in the dataflow path**: `tapOutputPort` / `tapInputPort` return `source.pipe(tap(emitTelemetry)).with({ meta })` and the result is `connect()`ed on the edge — so `output-emitted` / `input-received` fire as the run's values actually flow. The tap uses the `@rx-evo` typeguards (`isLoading` / `isError` / `isSuccess`) on the full `ResponseWithStatus` union, so it emits `state: 'pending'` (loading sentinel, fired by plain-Observable sources at connect), `'value'`, and `'error'` (the `@rx-evo` `ResponseError` payload) — full telemetry, no dropped states. The run is materialized by the graph itself: non-terminal nodes are driven by downstream `connect`, loops self-drive via a circular `connect`, and the runtime **subscribes only to end-node outputs** (outputs with no outgoing edge) to flush a linear graph. `wireScope` clears seeded defaults on wired ports first (BUG-2026-07-12b) and wraps only **regular** ports; **bypass** ports are `connect`ed raw (wrapping a bypass `StatefulConnection` with `pipe(tap)` would inject loading semantics and an empty initial emission). `isInactive` (disconnect/reset) is intentionally not emitted. |
| **Design flaw signal** | **Subscription order is observable event order.** A standalone `.subscribe()` for telemetry races the real dataflow because `@rx-evo` delivers a value to its first/lazy subscriber on subscribe. When event ordering carries meaning (the feed shows input→output, and the section order is the run's natural timeline), produce telemetry **on the connect path** (`pipe(tap)` with typeguards) rather than via a parallel watcher, so watching never materializes or reorders the run. Using `pipeValue(tap)` (values only) dropped error/`pending` telemetry; switching to `pipe(tap)` with `isLoading`/`isError`/`isSuccess` typeguards restores full lifecycle visibility.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Regression test**    | `packages/runtime/src/testing/workflows/port-event-order.workflow.test.ts` — linear chain `constant → delay → delay → delay`; each node's first `input-received` precedes its `output-emitted`. `packages/runtime/src/testing/workflows/delay-order.workflow.test.ts` — `string → delay(1000) → preview → finish`; delay's `output-emitted` precedes preview's and finish's (section order fixed). `packages/runtime/src/testing/workflows/signal-states.workflow.test.ts` — pending/error telemetry: async source emits `pending` → `value`; errored source emits `error`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

### BUG-2026-07-12b — default input value fires on a connected port, reversing work-log feed order

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Date**               | 2026-07-12                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Area**               | execution · runtime · feed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Symptom**            | In the example workflow (`string → router → delay(1000) → preview → finish`) the work-log feed ended `finish → preview → common-delay` in reverse; the delayed value (`"Hello"`) leaked into the feed _after_ `done`.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Repro**              | Run `demo-project/.langflower/workflows/example.json`. `delay` has `defaultValue: 0` but its real delay arrives via an edge from a `number(1000)` node (through the router).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Root cause**         | `materializeRuntimeNode` seeds every port default (`defaultInputsFromDefinition`) via `input.connect(of(value))` **unconditionally at node-add time**. At run start `wireScope` wires the real edge on top, but the seeded `0` is already on the connection, so `combineInputs([value, delay])` fires at t≈0 with `delay=0` → premature `of(value).pipe(delay(0))` → `preview` → `finish(stopsRun)` → `done`. The real `delay=1000` emission then arrives after `done`, producing the reversed tail. The runtime's own `applySeeds` already skips wired slots, but server-side default seeding bypassed that guard because it occurs at materialization, before the run. |
| **Fix**                | `runtime-runner.ts` `wireScope` now clears a seeded default on every input port an edge will drive — a pre-pass calls `connection.disconnect()` on each wired target **before** any edge is wired. A wired port is then sourced solely from its edge (combineLatest waits for the real value, so no early fire); an unconnected port keeps its seeded default. Harness/manual `connect`s on unwired ports are untouched.                                                                                                                                                                                                                                                 |
| **Design flaw signal** | **Default value applied even when the port is connected.** A port `defaultValue` is design-time metadata for the _unconnected_ case, but it was injected as a live connection value at materialization and never revoked when an edge superseded the port — a "default vs. actual" ownership mismatch at the runtime boundary. Rule: a default may drive a port **only when nothing is connected to it**.                                                                                                                                                                                                                                                                |
| **Regression test**    | `packages/runtime/src/testing/workflows/default-value.workflow.test.ts` — wired port drops the seeded default (output delayed by the edge); unconnected port keeps the default (immediate).                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

### BUG-2026-07-12a — `npm run dev` silently serves a stale pre-bundled `@langflower/shared` after a rebuild

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-12                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Area**               | ui · build                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Symptom**            | Browser console: `Uncaught SyntaxError: The requested module '/@fs/.../.angular/cache/.../vite/deps/@langflower_shared_langflower.js?v=...' does not provide an export named 'resolveUiSchemaOptions'` even right after `npm run dev` rebuilt every package (including `@langflower/shared`) and the new export was verified present in `packages/shared/dist/langflower.js`.                                                                                                                                                                                                           |
| **Repro**              | Add a new export to `@langflower/shared`'s entry module, import it from a `packages/ui` component, run `npm run dev` (which does a fresh `build-all` first), open the app — the import fails even though `dist/` is correct and `ng serve` recompiled with zero TS errors.                                                                                                                                                                                                                                                                                                              |
| **Root cause**         | Angular's Vite-based dev server pre-bundles `node_modules`/workspace dependencies and caches the bundles on disk at `packages/ui/.angular/cache/<version>/ui/vite/deps/`. That cache's invalidation key does not account for a local workspace package's `dist/` being rewritten in place (no `package.json`/lockfile change) — so a `dev.mjs` restart kept reusing the pre-rebuild `@langflower_shared_langflower.js` bundle, silently missing the new export until someone thought to delete the cache by hand.                                                                       |
| **Fix**                | `build/dev.mjs` now deletes `packages/ui/.angular/cache` after `build-all` and before starting `ng serve`, forcing Vite to re-optimize dependencies from the fresh build on every `npm run dev` invocation. (`build/clean.mjs` already removed this same directory, but that command is never called automatically as part of the normal dev loop.)                                                                                                                                                                                                                                     |
| **Design flaw signal** | **Cache invalidation blind to an out-of-band write.** A disk cache keyed off a coarse signal (lockfile/package.json hash) silently went stale when the thing it cached (`dist/`) was rewritten through a different path (a local build step) that the cache's own invalidation logic never observes. Any tool that pre-bundles/caches workspace-linked packages needs either its cache cleared by the same pipeline that rebuilds those packages, or a `--force` re-optimize flag wired into that pipeline — never left to be discovered per-developer after a confusing runtime error. |
| **Regression test**    | none — this is a dev-tooling cache-invalidation issue, not app logic; covered by the `dev.mjs` change itself (cache clear runs unconditionally on every `npm run dev`) rather than a test                                                                                                                                                                                                                                                                                                                                                                                               |

### BUG-2026-07-11d — ambiguous node-body inline fields; `defaultValue` and inline number/boolean edits never coerced

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-11                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Area**               | ui · canvas · server · node-definitions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Symptom**            | Three related issues in the old inline-editing path: (1) `DiagramInlineField` rendered node-body fields from `uiSchema` with no link to a specific input port, so a node with multiple inputs had no way to say _which_ input an inline field edited; (2) `InputParams.defaultValue` was declared on port configs but nothing ever read it into `node.inputs` on node creation, so unconnected inputs silently started as `undefined`; (3) editing a `number`/`boolean`-typed input inline persisted a **string** in `node.inputs`, since only `params` had server-side type coercion (`normalizeNodeParams`), not `inputs`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Repro**              | Add a `common-number`, leave `value` unconnected → `node.inputs.value` was `undefined` instead of the port's default. Edit that value inline → server stored `"1000"` (string) instead of `1000`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Root cause**         | Inline editing grew out of `uiSchema` (a node-level metadata array) instead of being a property of the port it edits, so the UI had to _guess_ the association between a body field and an input by name matching — this breaks as soon as a node has more than one editable input. Separately, `inputs` was added later than `params` and only partially mirrored its behavior: `defaultParamsFromDefinition`/`normalizeNodeParams` existed for `params`, but the equivalent `inputs` seeding/coercion was never written.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Fix**                | Deleted `DiagramInlineField`/`uiSchema`-driven body fields entirely. `InputParams.inline` is now a typed `InlineConfig` (`'text'` \| `'text-multiline'` \| `'boolean'` \| `'preview'` \| `'preview-markdown'` \| `'preview-code'` \| `{ type: 'select'\|'multiselect'\|'radio', options }`) that lives **on the port**, resolved by `resolveNodePorts` into `DiagramInputPortRow.inline`/`.value`/`.connected` and rendered by `lf-node-port-row` via `lf-inline-field.component.ts`. Portless literal nodes (`common-string`/`common-number`/`common-boolean`) were converted to a real `value` input port with `inline` set (no wire required) instead of a body field. Added `defaultInputsFromDefinition` (mirrors `defaultParamsFromDefinition`) and `normalizeNodeInputs` (mirrors `normalizeNodeParams`) in `apply-editor-mutation.ts`, applied on both `editor.addNode` and `editor.updateNode`. `inline: 'preview'` reads a live value from the new `NodePreviewValuesService`, which projects `runner.input-received` events — previously the Preview node's textarea had no execution-time data source at all beyond the wired-edge design-time value. |
| **Design flaw signal** | **Metadata attached to the wrong owner.** An editing affordance for an input's value was modeled as a node-level list (`uiSchema`) instead of a field on the input port it edits, so the UI layer had to reconstruct an association that the domain model should have carried directly. Also: **partial parity between two structurally identical fields** (`params` vs `inputs`) — whenever a new record-shaped field is added to mirror an existing one, grep for every helper on the original (`default*FromDefinition`, `normalize*`) and mirror all of them together, not just the parts exercised by the first feature that needed the new field.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Regression test**    | `packages/ui/src/app/diagram/tests/resolve-diagram-node-ports.test.ts` (inline/value/connected resolution), `packages/common-nodes/src/primitives/number/node.test.ts` (passthrough `value` input), `packages/server/src/workflow/apply-editor-mutation.test.ts` (`defaultInputsFromDefinition` / `normalizeNodeInputs`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

### BUG-2026-07-11c — router bypass port disappears on drag (frozen `graphInput` snapshot)

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-11                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Area**               | ui · canvas                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Symptom**            | Wire a new edge to a `common-router` node's bypass port (empty trailing slot appears, correct). Drag the router node afterwards → the empty slot silently disappears.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Repro**              | Load example workflow → wire an edge into the router's bypass channel → drag the router node → the trailing empty bypass slot is gone. See regression test in `dynamic-port-update.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Root cause**         | `editor.addEdges` patched the _live_ diagram node's `data.ports` via `buildDynamicPortUpdates` (BUG-2026-07-11's fix). But `editor.updateNodes` (fired by the drag → `editor.updateNode.requested` → server round trip) rebuilt that same node's ports via `persistedNodeToDiagram(node, palette, this.graphInput().edges)`. **`graphInput` is only the initial slice of the workflow, captured once from the first `workflow.current.snapshot` push at canvas mount — it is never refreshed by incremental `editor.addEdges`/`editor.deleteEdges` deltas.** So the rebuild silently reverted the patch applied one step earlier, using an edge list from _before_ the new edge existed. This is a structural staleness bug, not a one-off: two independent code paths (`buildDynamicPortUpdates` on live `Edge[]` vs. `resolveNodePorts` on frozen `graphInput().edges`) computed the same "real ports + 1 free slot" data from different, divergent sources, and any node-level update silently clobbered the first path's work. |
| **Fix**                | Deleted the entire client-side port-patch pipeline (`buildDynamicPortUpdates` and ~15 helpers in `bridge-diagram.service.ts`, `applyDynamicPortUpdates` in `flow-canvas.component.ts`). `LfNodeComponent` now derives `inputPortRows`/`outputPortRows`/`bypassPortRows` via `computed()`s reading `resolveNodePorts(portsConfig, nodeId, connectedEdges())`, where `connectedEdges` filters the **live** `NgDiagramModelService.edges()` signal by node id — recomputed automatically on every edge or node mutation, so nothing is cached on node `data` that can go stale. `persistedNodeToDiagram` no longer takes an `edges` argument or resolves ports at all. Added a doc comment on `FlowCanvasComponent.graphInput` and dedicated sections in `docs/NG_DIAGRAM.md` / `packages/ui/docs/DIAGRAM_CANVAS.md` spelling out that `graphInput` is init-only.                                                                                                                                                                     |
| **Design flaw signal** | **Frozen init-time snapshot read as if it were live state.** `graphInput` (`WorkflowPersistedGraph`, an `input.required<>()`) is bound once at mount and never updated by subsequent deltas, but nothing marked it as such — a second code path reached for it well after init, assuming it tracked current state. Whenever a component receives an initial data snapshot _and_ separately subscribes to incremental deltas for the same domain, the snapshot must either be explicitly one-shot-consumed (as here, only inside the seeding `computed()`) or actively kept in sync — never both "kept around" and "read elsewhere" without an explicit contract. This generalizes beyond ports: any future code reading `graphInput()` for "current" nodes/edges will reintroduce this exact class of bug.                                                                                                                                                                                                                         |
| **Regression test**    | `packages/ui/src/app/features/canvas/tests/dynamic-port-update.test.ts` — "keeps the trailing empty bypass slot after an unrelated node update (drag)"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

### BUG-2026-07-11b — session viewport not persisted when no workflow is active

| Field                  | Value                                                                                                                                                                                                                                                                                                                                         |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-11                                                                                                                                                                                                                                                                                                                                    |
| **Area**               | server · ws · session                                                                                                                                                                                                                                                                                                                         |
| **Status**             | open (deferred — unrelated to RuntimeEdge/node-data refactor in progress)                                                                                                                                                                                                                                                                     |
| **Symptom**            | `tests/integration/ws/ws-session-sync.ws.test.ts` — "broadcasts editor.viewport.delta and restores viewport on reconnect" fails: third client's `session.state.snapshot.viewport` is `undefined` instead of the viewport emitted by client A.                                                                                                 |
| **Repro**              | Connect client A (no `workflow.load`), emit `editor.viewport.requested`; connect client C and read its `session.state.snapshot`. Reproduces in isolation (`npx vitest run --project integration ... ws-session-sync.ws.test.ts`), not order-dependent.                                                                                        |
| **Root cause**         | `attach-langflower-bridge.ts`'s `editor.viewport.requested` handler only writes `raw.payload` into `session.activeWorkflow.graph.viewport` when `session.activeWorkflow !== null`; it never persists viewport at the session level, so a client that never loaded a workflow has no way to broadcast durable viewport state to later joiners. |
| **Fix**                | Deferred — needs a session-level (not per-workflow) viewport field, or the test needs a `workflow.load` before asserting reconnect-restore. Not touched by this change; no edge/port files were modified for this path.                                                                                                                       |
| **Design flaw signal** | **State ownership mismatch** — viewport is UI/session-scoped but was only ever wired into workflow-scoped graph state, so it silently no-ops outside an active workflow.                                                                                                                                                                      |
| **Regression test**    | `tests/integration/ws/ws-session-sync.ws.test.ts` (already exists, currently red — not authored in this change)                                                                                                                                                                                                                               |

### BUG-2026-07-11 — Dynamic port sync reads stale edges after addEdges

| Field                  | Value                                                                                                                                                                                                                                                                                               |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-11                                                                                                                                                                                                                                                                                          |
| **Area**               | ui · canvas                                                                                                                                                                                                                                                                                         |
| **Status**             | fixed (**superseded** — see BUG-2026-07-11c: this fix's own patch pipeline caused a follow-up staleness race and has since been deleted)                                                                                                                                                            |
| **Symptom**            | Multi-input and bypass ports did not grow/shrink correctly when edges were added or removed via bridge deltas; extra slots missing or not removed after disconnect                                                                                                                                  |
| **Repro**              | Add edge to merge node `value` input or bypass `ch` port; observe port rows. Remove last edge; spare slot should shrink. See `dynamic-port-update.test.ts`.                                                                                                                                         |
| **Root cause**         | `refreshNodePorts` read `diagramModel.edges()` after `addEdges`/`deleteEdges` (stale async middleware state), rebuilt all ports via `resolveNodePorts` with forbidden `Edge → persisted` conversion, and updated source nodes unnecessarily                                                         |
| **Fix**                | (historical) `buildDynamicPortUpdates` in `bridge-diagram.service.ts`; `FlowCanvasComponent.applyDynamicPortUpdates` uses pre-mutation `getConnectedEdges` per target node, patches via `getNodeDataForUpdate` + `updateNodeData` only. Deleted in BUG-2026-07-11c in favor of reactive derivation. |
| **Design flaw signal** | **Port sync mixed global stale model reads with full node rebuild** — dynamic slots must be derived from mutation payload + per-node connected edges before mutation, patched incrementally on `LfNodeData` lookups                                                                                 |
| **Regression test**    | `packages/ui/src/app/features/canvas/tests/dynamic-port-update.test.ts`                                                                                                                                                                                                                             |

### BUG-2026-06-28 — Live edge replace loses canvas edge until reload

| Field                  | Value                                                                                                                                                                                                                  |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-28                                                                                                                                                                                                             |
| **Area**               | ui · canvas · ws                                                                                                                                                                                                       |
| **Status**             | fixed                                                                                                                                                                                                                  |
| **Symptom**            | Replacing `string -> preview` with `delay -> preview` left stale/disconnected edge state in the live canvas; reloading the tab showed the correct workflow                                                             |
| **Repro**              | In example workflow, connect String to Preview, then connect Delay to the same Preview input; inspect WS `removeEdge.delta` + `addEdge.delta` and compare live canvas with post-reload canvas                          |
| **Root cause**         | Live canvas edge handling treated `removeEdge.delta` and `addEdge.delta` as independent imperative patches against ngDiagram's optimistic model instead of reconciling from the authoritative active workflow topology |
| **Fix**                | `BridgeDiagramService` now coalesces graph topology deltas and reconciles ngDiagram edges from `activeWorkflow.graph` through ngDiagram transaction/model-service lifecycle                                            |
| **Design flaw signal** | **Canvas topology mixed optimistic UI state with server facts** — server-owned graph topology must be projected atomically into ngDiagram, not repaired through local edge supersede heuristics                        |
| **Regression test**    | `packages/ui/src/app/features/canvas/flow-canvas.component.test.ts` — replace, second-tab delta, and optimistic edge reconciliation scenarios                                                                          |

### BUG-2026-06-26 — fromOutputPortId strips wrong prefix length (slice(3) vs slice(4))

| Field                  | Value                                                                                                                                                            |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-26                                                                                                                                                       |
| **Area**               | ui · canvas · port-id                                                                                                                                            |
| **Status**             | fixed                                                                                                                                                            |
| **Symptom**            | Edge draw sent `fromPort: [':value', 0]` instead of `['value', 0]`; output port names received a leading colon                                                   |
| **Repro**              | Draw edge between two nodes; inspect `editor.addEdge.requested` payload — `fromPort[0]` has `:` prefix                                                           |
| **Root cause**         | `fromOutputPortId` used `portId.slice(3)` but `out:` is 4 characters, leaving a leading `:`. `fromInputPortId` was correct (`in:` = 3 chars → `slice(3)` works). |
| **Fix**                | `diagram-port-id.ts`: `slice(3)` → `slice(4)` in `fromOutputPortId`                                                                                              |
| **Design flaw signal** | **Off-by-one in string prefix removal** — `in:` and `out:` have different lengths but shared the same slice index; should use constant or `replace`              |
| **Regression test**    | `packages/ui/src/app/features/canvas/flow-canvas.component.test.ts` — "emits editor.addEdge.requested with correct from/to ports"                                |

### BUG-2026-06-25 — Dropped node vanishes (duplicate editor.addNode)

| Field                  | Value                                                                                                                                                                                 |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-25                                                                                                                                                                            |
| **Area**               | ui · ws · canvas                                                                                                                                                                      |
| **Status**             | fixed                                                                                                                                                                                 |
| **Symptom**            | Drop palette node → brief flash → node removed; WS shows second `editor.addNode.settled: false` then `editor.removeNode`                                                              |
| **Repro**              | Drop any node on canvas; ngDiagram `onChange` fires twice before first add settles                                                                                                    |
| **Root cause**         | `EditorSyncService` marked nodes synced only after async `settled`; duplicate `handleModelChange` enqueued a second `addNode` with the same id → runtime rejected → `revertAddedNode` |
| **Fix**                | Track `pendingNodeIds` / `pendingEdgeIds` synchronously on enqueue; abort server add if canvas node gone before settled                                                               |
| **Design flaw signal** | Optimistic UI sync treated async acknowledgment as the only in-flight guard; diagram emits multiple change events per gesture                                                         |
| **Regression test**    | none — UI service; manual WS trace                                                                                                                                                    |

### BUG-2026-06-25 — workflow.list.snapshot empty after save (corrupt file in catalog)

| Field                  | Value                                                                                                                                |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| **Date**               | 2026-06-25                                                                                                                           |
| **Area**               | server                                                                                                                               |
| **Status**             | fixed                                                                                                                                |
| **Symptom**            | Save succeeds but following `workflow.list.snapshot` has `workflows: []`                                                             |
| **Repro**              | Place invalid JSON in `.langflower/workflows/`; save any valid workflow via WS                                                       |
| **Root cause**         | `WorkflowService.list()` used `Promise.all` over all files; one parse failure rejected the batch and the outer `catch` returned `[]` |
| **Fix**                | Per-file try/catch in `workflow.service.ts`; skip invalid files, return valid entries                                                |
| **Design flaw signal** | Catalog listing treated filesystem as atomic all-or-nothing; partial corruption must not hide valid workflows                        |
| **Regression test**    | `packages/server/src/workflow/workflow.service.test.ts`; `tests/integration/ws/workflows.ws.test.ts`                                 |

### BUG-2026-06-25 — Workflow picker empty after save (late projection lifecycle)

| Field                  | Value                                                                                                                                                                                                                                                      |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-25                                                                                                                                                                                                                                                 |
| **Area**               | ui · ws                                                                                                                                                                                                                                                    |
| **Status**             | fixed                                                                                                                                                                                                                                                      |
| **Symptom**            | Workflow file saved on disk but picker `<select>` stayed empty                                                                                                                                                                                             |
| **Repro**              | Lazy editor route loads after WebSocket bootstrap; save or open picker                                                                                                                                                                                     |
| **Root cause**         | WebSocket opened in `LangflowerBridgeService` constructor before editor shell and projections existed; cold `shareReplay` + late subscribers missed bootstrap; catalog merged `snapshot.workflows` instead of `workflow.list.requested`                    |
| **Fix**                | Lazy `connect()` in `EditorPageComponent.ngAfterViewInit`; `LangflowerBridgeService.from()` for server→client observables (wait for client); `workflow.list.requested` on `session.ready`; catalog from `workflow.list.snapshot` + `refresh()` after ready |
| **Design flaw signal** | Transport must start when the editor shell is ready, not at app bootstrap; projections bind via `from()`, intents via `raw` after `connect()`                                                                                                              |
| **Regression test**    | `tests/integration/ws/workflows.ws.test.ts`                                                                                                                                                                                                                |

### BUG-2026-06-18 — Agent tool loop step 2+ InvalidPromptError (legacy ModelMessage)

| Field                  | Value                                                                                                                                                                                                                     |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-18                                                                                                                                                                                                                |
| **Area**               | execution · server · runtime · agent                                                                                                                                                                                      |
| **Status**             | fixed                                                                                                                                                                                                                     |
| **Symptom**            | After first successful tool call, agent loop failed with `AI_InvalidPromptError` (`role: tool` message had string `content`). Downstream Ask User showed unhandled `statefulObservable` errors.                           |
| **Repro**              | Run `coder` workflow with LM Studio; prompt that triggers a tool call (e.g. `read spec.md`); observe failure on second `streamText` iteration.                                                                            |
| **Root cause**         | Agent loop appended legacy OpenAI messages (`toolCalls`, string tool `content`). Tool invocations ran imperatively in server outside runtime `statefulObservable` error lifecycle.                                        |
| **Fix**                | `build-ai-sdk-messages.ts` — ModelMessage content-parts + `system` option; `runAgentToolCalls` in `@langflower/runtime` wraps each tool step in `statefulObservable`; `execute-agent.ts` delegates tool batch to runtime. |
| **Design flaw signal** | **Agent loop used OpenAI JSON history while LLM client moved to AI SDK v6** — message serialization and tool execution must share runtime lifecycle and error$ contract.                                                  |
| **Regression test**    | `packages/server/src/llm/build-ai-sdk-messages.test.ts`, `packages/runtime/src/agent/run-agent-tool-calls.test.ts`                                                                                                        |

### BUG-2026-06-18 — Agent tools rejected by OpenAI-compatible servers (missing parameters.type)

| Field                  | Value                                                                                                                                                                                                                                     |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-18                                                                                                                                                                                                                                |
| **Area**               | execution · server · agent                                                                                                                                                                                                                |
| **Status**             | fixed                                                                                                                                                                                                                                     |
| **Symptom**            | Agent run against LM Studio returned `AI_APICallError: Bad Request` with Zod `invalid_union_discriminator` on all tools (`parameters.type` expected `'object'`). Workflow failed with `NODE_EXECUTION_FAILED` / `NoOutputGeneratedError`. |
| **Repro**              | Run `coder` workflow with `lmstudio/local-model` and LM Studio on `http://127.0.0.1:1234/v1`; agent node with built-in harness tools.                                                                                                     |
| **Root cause**         | `buildOpenAiToolSchemas` returned a raw OpenAI tool **array**, passed to AI SDK v6 `streamText({ tools })`. SDK treated array indices as tool names and emitted empty parameter schemas without `type: 'object'`.                         |
| **Fix**                | `buildAiSdkToolSet` in `packages/server/src/llm/build-ai-sdk-tool-set.ts` — ToolSet via `tool()` + `jsonSchema()` + `inputSchema`; used in `execute-agent.ts` and `execute-review.ts`.                                                    |
| **Design flaw signal** | **Assumed raw OpenAI tool JSON is accepted by AI SDK `streamText`** — SDK v6 requires ToolSet + `inputSchema`; provider-specific serialization belongs in one adapter layer.                                                              |
| **Regression test**    | `packages/server/src/llm/build-ai-sdk-tool-set.test.ts`                                                                                                                                                                                   |

### BUG-2026-06-18 — Interactive HITL feedback loop auto-completed after N replies

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Date**               | 2026-06-18                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Area**               | execution · runtime · server · ws · ui · hitl                                                                                                                                                                                                                                                                                                                                                                      |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Symptom**            | Agent ↔ Ask User chat loop stopped after ~2–3 user replies (`hi → 1 → 2 → 3 → workflow stopped`). UI showed terminal completed; next agent question never arrived.                                                                                                                                                                                                                                                 |
| **Repro**              | Run `untitled` or `llm-hitl-inline` with mock/real LLM; answer repeatedly via Ask User; observe `execution.progress: completed` before the next `execution.awaiting_input`.                                                                                                                                                                                                                                        |
| **Root cause**         | Runtime treated interactive feedback loops like batch jobs: `collectRunResult()` ran when the graph looked idle. Settle raced with streaming LLM completion and Ask User `reply` propagation. Mitigations (`maxSettlePasses`, microtask/timer defer in batch settle, value-version waits) were timing heuristics without a stable “loop finished” signal.                                                          |
| **Fix**                | Classify interactive loops via `hasInteractiveHitlFeedbackLoop` (Ask User `reply` → agent `feedback`, scoped edges only). Skip auto `collectActiveRunResult()` for those runs; terminal completion only on `execute.stop`. Review accept loops unchanged. Terminal progress carries `runId`; UI filters stale events. ADR: [ADR-015](ADR.md#adr-015--interactive-hitl-feedback-loops-end-on-stop-not-idle-settle). |
| **Design flaw signal** | **Completion semantics conflated batch idle with conversational HITL** — a multi-turn user loop is not a finite DAG job; it needs an explicit end signal (Stop), not settle guesses.                                                                                                                                                                                                                               |
| **Regression test**    | `packages/runtime/src/workflow-runner.test.ts`, `tests/integration/ws/execute-llm-hitl.ws.test.ts`, `packages/shared/src/execution/feedback-edges.test.ts`                                                                                                                                                                                                                                                         |

### BUG-2026-06-18 — Legacy ReactivePortBus: switchMap + hot inputs never settled runs

| Field                  | Value                                                                                                                                                                                                                                                     |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-18                                                                                                                                                                                                                                                |
| **Area**               | execution · shared · reactive                                                                                                                                                                                                                             |
| **Status**             | fixed (stack deleted)                                                                                                                                                                                                                                     |
| **Symptom**            | Review/agent reactive runs hung in legacy tests: `waitForIdle` timed out after feedback emit; `combineLatest` on hot bus inputs never completed inner `switchMap` runs.                                                                                   |
| **Repro**              | Wire review node through `createReactiveNodeRuntime` + `ReactivePortBus`; emit task/result; observe feedback port emits but tracker stays pending until timeout.                                                                                          |
| **Root cause**         | Legacy adapter used `switchMap` per input event without run-scoped lifecycle aligned to terminal port emit; hot `Subject` inputs never `complete`; `Observable.complete` on output was required for idle in tests but production hot bus never completes. |
| **Fix**                | Production path uses `@langflower/runtime` (`ReactiveRunTracker`, `completeReactiveRun` in node `finalize` via `withReactiveRunLifecycle`). Legacy bus stack removed from shared (Phase 8).                                                               |
| **Design flaw signal** | **Two execution adapters** duplicated lifecycle rules; hot-input reactive graphs need explicit run boundaries, not Observable `complete` on upstream.                                                                                                     |
| **Regression test**    | `packages/runtime/src/common-nodes/review-nodes.test.ts`, `packages/runtime/src/adapters/reactive-node-adapter.test.ts`                                                                                                                                   |

### BUG-2026-06-18 — Mock agent cached script / reset callIndex broke mock matching

| Field                  | Value                                                                                                                                                                                                                                                  |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Date**               | 2026-06-18                                                                                                                                                                                                                                             |
| **Area**               | execution · server · agent · mock-llm                                                                                                                                                                                                                  |
| **Status**             | fixed                                                                                                                                                                                                                                                  |
| **Symptom**            | Bootstrap plan and agent mock integration tests returned default mock text (`Mock response`, bootstrap default greeting) instead of scripted tool-loop / plan content. LLM+HITL feedback loop timed out waiting for second `execution.awaiting_input`. |
| **Repro**              | Run `plan` workflow with seeded `mock-llm.json.tpl`, or `llm-hitl` with `callIndex` + `hasFeedback` entries; observe wrong mock content or hung HITL loop.                                                                                             |
| **Root cause**         | `createAgentExecuteFn` cached `scriptPromise` (stale script) and/or reset `callIndex` to 0 on every agent invocation, so multi-step tool loops and feedback-loop second calls matched the wrong mock entry.                                            |
| **Fix**                | Reload `mock-llm.json` on each mock agent call; persist per-run `llmCallIndex` as `startCallIndex` and advance from `executeMockAgentLoop` `finalCallIndex`. File: `packages/server/src/agent/execute-agent.ts`.                                       |
| **Design flaw signal** | **Mock LLM state split** — script freshness and call-index cursor are separate concerns; caching one while resetting the other breaks ordered `when.callIndex` scripts.                                                                                |
| **Regression test**    | `tests/integration/bootstrap-plan-mock.test.ts`, `tests/integration/ws/execute-agent-mock.ws.test.ts`, `tests/integration/ws/execute-llm-hitl.ws.test.ts`                                                                                              |

### BUG-2026-06-18 — WS runId and runtime runId diverge; HITL resume no-op

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-18                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Area**               | execution · server · runtime · ws · hitl                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Symptom**            | WS HITL flows time out after `execution.awaiting_input`: `execute.user_input` accepted but run never completes; integration tests fail on terminal `completed`. In-process runtime unit tests passed.                                                                                                                                                                                                                                                       |
| **Repro**              | 1) `execute.start` on a workflow with Agent → Ask User. 2) Wait for `execution.awaiting_input`. 3) `execute.user_input` with `runId` from WS ack. 4) Observe run hangs until client timeout.                                                                                                                                                                                                                                                                |
| **Root cause**         | `ws-handler.ts` generated a client `runId` and passed it to `WorkflowExecutorService`, but `workflow-runner.ts` always called `crypto.randomUUID()` for the runtime run. `resolveUserInput` required `activeRun.runId === runId`; mismatch meant `runtime.resumeUserInput` was skipped and only `userInputGateway.resolveUserInput` ran — the gateway promise is fire-and-forget and does **not** unblock `runtime.waitForUserInput()` on the hitl channel. |
| **Fix**                | Add optional `runId` to `WorkflowRunOptions`; pass `effectiveRunId` from `run-workflow-core.ts` into `runtime.run()`. Resume via `activeRun.resumeUserInput` when executor `activeRunId` matches (drop redundant runtime UUID check). Files: `packages/runtime/src/contracts.ts`, `packages/runtime/src/workflow-runner.ts`, `packages/server/src/runtime/run-workflow-core.ts`, `packages/server/src/services/workflow-executor.service.ts`.               |
| **Design flaw signal** | **Run identity was split across layers** — WS session id, executor session id, and runtime execution id were assumed equal without a single owner or propagation contract. HITL has two wait paths (gateway + runtime channel); only one was wired to resume.                                                                                                                                                                                               |
| **Regression test**    | `tests/integration/ws/execute-hitl-complete.ws.test.ts`, `tests/integration/ws/execute-llm-hitl.ws.test.ts`, `tests/integration/ws/execute-agent-mock.ws.test.ts` (ask_user HITL); `packages/server/src/runtime/run-workflow.test.ts` (in-process HITL)                                                                                                                                                                                                     |

### BUG-2026-06-18 — runtime run result collected before node runners start

| Field                  | Value                                                                                                                                                                                                                                  |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-18                                                                                                                                                                                                                             |
| **Area**               | execution · runtime                                                                                                                                                                                                                    |
| **Status**             | fixed                                                                                                                                                                                                                                  |
| **Symptom**            | Workflow run finished immediately with empty `nodeOutputs` or wrong terminal status although nodes were still executing.                                                                                                               |
| **Repro**              | Run any workflow through `@langflower/runtime` after server migration; observe `execution.done` resolving before batch/reactive runners produce outputs.                                                                               |
| **Root cause**         | `collectRunResult()` was invoked during `run()` setup before `runner.start()` loop; `done` could resolve against an empty settled graph.                                                                                               |
| **Fix**                | Start all runners first; call `collectActiveRunResult()` after the start loop. On HITL resume, re-collect via `collectActiveRunResult()` instead of reusing stale `pendingRunResult`. File: `packages/runtime/src/workflow-runner.ts`. |
| **Design flaw signal** | **Run lifecycle ordering** — result aggregation must be tied to runner start + interrupt/resume state, not to graph materialization alone.                                                                                             |
| **Regression test**    | `packages/runtime/src/workflow-runner.test.ts`; integration WS smoke tests (`execute-simple.ws.test.ts`, etc.)                                                                                                                         |

### BUG-2026-06-18 — runtime WS bridge emitted agent completed on every stream chunk

| Field                  | Value                                                                                                                                                                                                                                                 |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-18                                                                                                                                                                                                                                            |
| **Area**               | execution · server · ws · agent                                                                                                                                                                                                                       |
| **Status**             | fixed                                                                                                                                                                                                                                                 |
| **Symptom**            | Multiple `execution.node.completed` events for one Agent run; downstream/toolLog outputs missing or stale in the final completed payload.                                                                                                             |
| **Repro**              | Run Agent with streaming mock LLM over WS; watch `execution.node.completed` for `llm-1` (or agent node id) — one event per stream chunk instead of one at response completion.                                                                        |
| **Root cause**         | `attachRuntimeExecutionSink` / `bridgeOutputPort` called `onNodeCompleted` whenever a port left `pending` after streaming, including non-terminal stream ports (`thoughts`, `draftResponse`).                                                         |
| **Fix**                | Emit `onNodeCompleted` only when a **reactive completion port** (e.g. `response`) transitions `pending → false`, using shared per-node accumulated outputs so `toolLog` is included. File: `packages/server/src/runtime/runtime-execution-events.ts`. |
| **Design flaw signal** | **Stream port lifecycle ≠ node completion** — WS/UI “node completed” must follow semantic completion ports, not every `pending$` edge on streaming outputs.                                                                                           |
| **Regression test**    | `tests/integration/ws/execute-agent-mock.ws.test.ts`; WS client `findNodeCompleted` now takes the **last** matching event (`tests/integration/ws/langflower-ws-client.ts`)                                                                            |

### BUG-2026-06-18 — partial run reused stale output-bus values (router / mustFresh)

| Field                  | Value                                                                                                                                                                                                                                                                                                              |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Date**               | 2026-06-18                                                                                                                                                                                                                                                                                                         |
| **Area**               | execution · runtime · shared · router                                                                                                                                                                                                                                                                              |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                              |
| **Symptom**            | Partial run from a Preview/Router branch used outputs from a previous global run; router partial run skipped re-execution.                                                                                                                                                                                         |
| **Repro**              | Global run on dual-channel Router workflow; then `execute.start` with `fromNodeId` on one branch Preview — wrong cached values or router not in executable set.                                                                                                                                                    |
| **Root cause**         | (1) `RuntimeOutputBus` retained prior run values for nodes in partial scope. (2) `buildPartialRunPlan` / `mustFresh` did not include upstream Router nodes on selective downstream walks (fixed via `getSelectiveDownstreamNodeIds` including router nodes).                                                       |
| **Fix**                | `outputBus.resetRunOutputs(scope.executableNodeIds)` at run start. Router nodes included in partial `mustFresh` set. Files: `packages/runtime/src/output-bus.ts`, `packages/runtime/src/workflow-runner.ts`, `packages/shared/src/execution/partial-run-plan.ts`, `packages/shared/src/router/router-channels.ts`. |
| **Design flaw signal** | **Output bus is global mutable state** — partial runs need explicit scope reset and instance topology (Router channels) in the fresh set, not only batch seed cache.                                                                                                                                               |
| **Regression test**    | `tests/integration/ws/execute-router.ws.test.ts`; `packages/shared/src/execution/partial-run-plan.test.ts`                                                                                                                                                                                                         |

### BUG-2026-06-17 — port info popover vertical anchor misaligned

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-17                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Area**               | ui · canvas                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Symptom**            | Port hover popover opened with the **top** of the Disconnect button aligned to the port dot instead of the button **center**; earlier attempts shifted the whole popover too high or too low.                                                                                                                                                                                                                                                                                                                                                                                                |
| **Repro**              | Hover a wired input or output port on a canvas node; observe Disconnect button vs port dot vertical alignment.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Root cause**         | Vertical placement used two CSS vars (`--lf-port-dot-center-y`, `--lf-port-popover-button-center-y`) but the button offset was measured on the Angular **component host** (`<lf-port-info-popover>`) while `top` / `transform` applied to the inner **`.lf-port-info-popover`** div. The host is a zero-height inline box at the port; the inner div is absolutely positioned — so `translateY` used the wrong reference and only ~half the needed offset (button top ≈ port center). Earlier iterations also centered the whole panel or double-counted `offsetTop` along the parent chain. |
| **Fix**                | Measure and set `--lf-port-popover-button-center-y` on `#popoverRoot` (same element as `transform`); clear `transform` on that element during measure; `display: contents` on the host. Port dot center still set on `.lf-port-hover-host` via `getBoundingClientRect`. Files: `lf-port-info-popover.component.ts`, `lf-port-hover-zone.component.ts`, `node-port-layout.css`.                                                                                                                                                                                                               |
| **Design flaw signal** | **Measurement target ≠ styled element** — with Angular wrapper hosts, layout/position CSS on template roots must use the same DOM node for JS measurement and for CSS variables that drive that layout.                                                                                                                                                                                                                                                                                                                                                                                      |
| **Regression test**    | `none` (visual canvas alignment; no stable DOM fixture for ng-diagram port dots)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

### BUG-2026-06-17 — workflow output shown as error in work log

| Field                  | Value                                                                                                                                                         |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-17                                                                                                                                                    |
| **Area**               | ui · execution · work log                                                                                                                                     |
| **Status**             | fixed                                                                                                                                                         |
| **Symptom**            | After running the constant cluster, concatenated output (e.g. `Hello Langflower` ×4 from Collect) appears in red error styling at the bottom of the work log. |
| **Repro**              | Run `example.json` constant → collect → … cluster; observe work log footer after successful completion.                                                       |
| **Root cause**         | `handleProgress` stored `payload.output` in `runResult$`; `runError` treats any value other than known success sentinels as a failure banner.                 |
| **Fix**                | On `completed` / `completed_with_errors`, set `runResult` to status text only, not workflow output.                                                           |
| **Design flaw signal** | **`runResult` mixed two roles** — terminal status banner vs final workflow output string.                                                                     |
| **Regression test**    | `none` (UI banner; output belongs in system timeline entries per node)                                                                                        |

### BUG-2026-06-16 — partial run after cancelled HITL runs wrong cluster

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-16                                                                                                                                                                                                                                                                                                                                           |
| **Area**               | execution · server · ui · hitl                                                                                                                                                                                                                                                                                                                       |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                |
| **Symptom**            | After Run on LLM → Stop while Ask User waits → Run another disconnected cluster, the LLM cluster runs again instead of only the clicked cluster.                                                                                                                                                                                                     |
| **Repro**              | 1) `runFromNode` on LLM. 2) Stop during `execution.awaiting_input`. 3) `runFromNode` on a node in a separate connected component (e.g. Constant in `example.json`).                                                                                                                                                                                  |
| **Root cause**         | (1) `WorkflowExecutorService` started a new session without awaiting the previous one — two runs overlapped and WS events interleaved. (2) `runFeedbackReruns` had no partial-run scope — seeded LLM + Ask User outputs could trigger LLM rerun after an unrelated cluster finished. (3) UI kept stale `mustFresh` outputs in the seed after cancel. |
| **Fix**                | Serialize runs in `workflow-executor.service.ts` (`finishActiveRun` before start; session-safe `finally`). Scope `runFeedbackReruns` to partial-run `scope`. Strip `mustFresh` outputs on cancel in `workflow-execution.service.ts`; auto-`stopRun` before new `startRun`. `UserInputGateway` rejects with `RunCancelledError`.                      |
| **Design flaw signal** | **Run lifecycle was implicit** — no single owner for “one active run”, and global side effects (`runFeedbackReruns`, seed cache) ignored partial-run boundaries. Execution model doc claimed isolation that the executor did not enforce.                                                                                                            |
| **Regression test**    | `packages/server/src/services/workflow-executor.service.test.ts` — “waits for a cancelled HITL run…”, “does not feedback-rerun LLM outside partial run scope”                                                                                                                                                                                        |

### BUG-2026-06-16 — duplicate “Run cancelled” in work log

| Field                  | Value                                                                                                                                  |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-16                                                                                                                             |
| **Area**               | ui · execution                                                                                                                         |
| **Status**             | fixed                                                                                                                                  |
| **Symptom**            | Stopping a run showed “Run cancelled” twice in the work log.                                                                           |
| **Repro**              | Stop workflow while Ask User (or any node) is active; observe work log.                                                                |
| **Root cause**         | Cancel surfaced both as `execution.node.failed` on the waiting node and as `execution.progress` failed banner text.                    |
| **Fix**                | Shared `RUN_CANCELLED_MESSAGE`; skip failed-node timeline append for cancel; single system line via `appendRunCancelledNotice()`.      |
| **Design flaw signal** | **Terminal outcomes are multiplexed** — node failure and run progress are separate channels without a shared “cancelled” discriminant. |
| **Regression test**    | `none` (UI behaviour; covered indirectly by execution cancel flow)                                                                     |

### BUG-2026-06-17 — stream output ports blocked on canvas

| Field                  | Value                                                                                                                                  |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-17                                                                                                                             |
| **Area**               | ui · shared · execution                                                                                                                |
| **Status**             | fixed                                                                                                                                  |
| **Symptom**            | Cannot wire from LLM/Agent `thoughts`, `draftResponse`, or other `stream: true` outputs.                                               |
| **Repro**              | Drag edge from Agent `draftResponse` to Preview `text` — connection rejected.                                                          |
| **Root cause**         | `diagram.config.ts` rejected all `isStreamOutputPort` sources; `normalizeNodeOutput` omitted stream ports from batch `nodeOutputs`.    |
| **Fix**                | Validate stream outputs by wire type only; persist final accumulated stream value in `nodeOutputs` for downstream `resolveNodeInputs`. |
| **Design flaw signal** | **Stream conflated with wire type** — runtime streaming flag was treated as “not a real port” in canvas and batch cache.               |
| **Regression test**    | `packages/shared/src/validators/connection-validator.test.ts`, `packages/shared/src/normalize-node-output.test.ts`                     |

### BUG-2026-06-16 — duplicate empty LLM streaming placeholder in work log

| Field                  | Value                                                                                                               |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-16                                                                                                          |
| **Area**               | ui · execution · llm                                                                                                |
| **Status**             | fixed                                                                                                               |
| **Symptom**            | Two empty assistant placeholders above the LLM node during streaming.                                               |
| **Repro**              | Run LLM node; watch work log while streaming starts.                                                                |
| **Root cause**         | `onNodeRunning` fired twice per attempt (pre-execute and `onRunning` callback inside `executeCommonNode`).          |
| **Fix**                | Reuse existing assistant message id on second running event; `removeOrphanLlmPlaceholders` on complete.             |
| **Design flaw signal** | **Duplicate lifecycle hooks** — batch executor and node runtime both signal “running” without idempotency contract. |
| **Regression test**    | `none` (UI timeline; server emits duplicate running today by design — UI dedupes)                                   |

### BUG-2026-06-16 — WorkflowCatalogService NG0201 in editor

| Field                  | Value                                                                                                |
| ---------------------- | ---------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-16                                                                                           |
| **Area**               | ui · angular di                                                                                      |
| **Status**             | fixed                                                                                                |
| **Symptom**            | `NG0201` — no provider for `WorkflowSyncService` when using workflow picker.                         |
| **Repro**              | Open editor with workflow picker after catalog was `providedIn: 'root'`.                             |
| **Root cause**         | Catalog service injected editor-scoped `WorkflowSyncService` from root scope.                        |
| **Fix**                | Provide `WorkflowCatalogService` on `EditorPageComponent` instead of root.                           |
| **Design flaw signal** | **Editor-scoped state** was not documented; root singletons must not depend on page-scoped services. |
| **Regression test**    | `none` (runtime DI; manual editor smoke)                                                             |

### BUG-2026-06-17 — Preview ▶ Run clears output, no re-eval (router-fed)

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-17                                                                                                                                                                                                                                                                                                                                                    |
| **Area**               | execution · server · ui                                                                                                                                                                                                                                                                                                                                       |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                         |
| **Symptom**            | Clicking ▶ on a Preview node (especially the bottom branch of a dual-channel Router) cleared output or did nothing after the run.                                                                                                                                                                                                                             |
| **Repro**              | String → Router (`ch` + `ch@1`) → two Previews; global run; then ▶ on bottom Preview.                                                                                                                                                                                                                                                                         |
| **Root cause**         | **Three design flaws:** (1) dual execution paths — partial run relied on reactive bus while batch topo skipped bus-fed nodes; (2) Router uses **per-node** `routerChannels` but runner used static type definition (`['ch']` only), so `ch@1` inputs never resolved; (3) UI seed/`mustFresh` contract was split across strip-on-start vs replace-on-complete. |
| **Fix**                | Partial runs: **batch-only path** — materialize Router passthrough into `nodeOutputs` with `resolveWorkflowNodeDefinition`; execute all `mustFresh` batch nodes from cache. Global runs unchanged (bus). UI merges partial-run `nodeOutputs` on completion.                                                                                                   |
| **Design flaw signal** | **Instance topology vs type registry** — dynamic nodes (Router) cannot share a static `NodeDefinition`; partial run must not depend on bus side-effects.                                                                                                                                                                                                      |
| **Regression test**    | `packages/server/src/services/workflow-executor.service.test.ts` (dual-channel bottom preview); `packages/shared/src/execution/materialize-passthrough-outputs.test.ts`                                                                                                                                                                                       |

### BUG-2026-06-26b — patchNodeFromServer mutates in-place, Angular signals skip re-render

| Field                  | Value                                                                                                                                                                                                                                                                                                                                       |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-26                                                                                                                                                                                                                                                                                                                                  |
| **Area**               | ui · bridge-diagram                                                                                                                                                                                                                                                                                                                         |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                       |
| **Symptom**            | Edge disconnects visually after second node move — node stays at old position while edge routes to new position.                                                                                                                                                                                                                            |
| **Repro**              | Connect two nodes → move one node twice → edge appears detached from node.                                                                                                                                                                                                                                                                  |
| **Root cause**         | `patchNodeFromServer` mutated the node object in-place and returned the same reference. `mergeServerNodes` returned a new array with same node references. `SignalModelAdapter.nodes` signal uses `Object.is` — same reference = no re-render. Node visual stayed at old position while edge routing middleware re-ran on updated position. |
| **Fix**                | `patchNodeFromServer` and `patchEdgeFromServer` now return a new spread object when data changes, or short-circuit returning the original reference when nothing changed.                                                                                                                                                                   |
| **Design flaw signal** | **Signal identity vs mutation** — Angular signals use `Object.is` for dirty-checking. Mutable updates that return the same reference are invisible to signals. Always return new objects for signal-tracked state.                                                                                                                          |
| **Regression test**    | `packages/ui/src/app/features/canvas/flow-canvas.component.test.ts` (mergeServerNodes returns new refs when changed, same refs when unchanged); `packages/ui/src/app/services/bridge-diagram.service.ts` exports `mergeServerNodes`, `mergeServerEdges`, `patchNodeFromServer`, `patchEdgeFromServer`                                       |

### BUG-2026-06-26c — editor.updateNode.delta bypasses edgesRoutingMiddleware

| Field                  | Value                                                                                                                                                                                                                                                                                                                                             |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-26                                                                                                                                                                                                                                                                                                                                        |
| **Area**               | ui · bridge-diagram                                                                                                                                                                                                                                                                                                                               |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                             |
| **Symptom**            | Edges freeze at stale SVG paths after node drag — second drag makes edge visually disconnect from node because edge routing middleware never runs.                                                                                                                                                                                                |
| **Repro**              | Connect two nodes with an edge → drag source node → edge updates (via internal moveNodesBy) → server confirms with updateNode.delta → drag source node again → edge SVG path doesn't update, appears disconnected.                                                                                                                                |
| **Root cause**         | `editor.updateNode.delta` handler used `adapter.updateNodes()` and/or full `persistedNodeToDiagram()` rebuilds, bypassing or perturbing the middleware pipeline. Full node payloads re-triggered port bulk middleware (`deletePortsBulk` / `updatePortsBulk`), invalidating `measuredPorts` so the second `updateNodes` could not re-route edges. |
| **Fix**                | Route delta patches through `NgDiagramModelService.updateNodes()`. Compare delta node with prior state (`activeWorkflow` or on-canvas node): position-only → partial `{ id, position, size? }`; params/inputs/label/port-slot changes → include `data`/`type`/`autoSize` from `persistedNodeToDiagram`. Removed private `flowCore` hack.          |
| **Design flaw signal** | **Two-path model update** — `SignalModelAdapter` (data layer) and `FlowCore` (command/middleware layer) are separate. Direct adapter writes skip middleware. Server-pushed updates must use model service APIs; avoid full diagram node rebuilds when only geometry changed.                                                                      |
| **Regression test**    | `packages/ui/src/app/features/canvas/flow-canvas.component.test.ts` — "connected edge updates after second node drag"                                                                                                                                                                                                                             |

### BUG-2026-06-26d — editor deltas unicast + snapshot broadcast broke multi-tab canvas sync

| Field                  | Value                                                                                                                                                                                                                                                                                 |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-26                                                                                                                                                                                                                                                                            |
| **Area**               | server · shared protocol · ui · bridge-diagram                                                                                                                                                                                                                                        |
| **Status**             | fixed                                                                                                                                                                                                                                                                                 |
| **Symptom**            | Two browser tabs: tab A moves a node — tab B moves the node but connected edges stay frozen at stale SVG paths.                                                                                                                                                                       |
| **Repro**              | Open two tabs on the same workflow → connect nodes with an edge in tab A → drag a node in tab A → tab B receives only `workflow.current.snapshot`, not the node delta; snapshot applies positions via `adapter.updateNodes()` (no edge routing).                                      |
| **Root cause**         | Server sent `editor.*.settled` only to the initiating client (`clientEmit`) and broadcast a full `workflow.current.snapshot` after every editor mutation. Remote tabs never received the incremental fact; snapshot path bypassed `edgesRoutingMiddleware`.                           |
| **Fix**                | Rename `editor.*.settled` → `editor.*.delta`; broadcast deltas to all tabs via `bridgeEmit`; remove `broadcastWorkflowCurrentSnapshot` from editor handlers. Reserve `workflow.current.snapshot` for load/save/rename/connect. UI patches `canvasState.activeWorkflow` on each delta. |
| **Design flaw signal** | **Event-sourcing vs snapshot transport** — incremental canvas edits must broadcast the same delta to every tab; full snapshot replace is for bootstrap/load/save only, not live moves.                                                                                                |
| **Regression test**    | `tests/integration/ws/editor.ws.test.ts` — "broadcasts editor.updateNode.delta to all tabs without workflow snapshot"                                                                                                                                                                 |

### BUG-2026-06-26e — palette drop crashes after addNode routed through diagramModel.addNodes

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                                   |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-26                                                                                                                                                                                                                                                                                                                                                              |
| **Area**               | ui · bridge-diagram                                                                                                                                                                                                                                                                                                                                                     |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                                   |
| **Symptom**            | Drop from palette → `TypeError: Cannot use 'in' operator to search for 'label' in undefined` in `lf-node`; node may flash without label/ports.                                                                                                                                                                                                                          |
| **Repro**              | Open editor → drop any palette node after protocol switch to `editor.addNode.delta` (no post-mutation snapshot).                                                                                                                                                                                                                                                        |
| **Root cause**         | Two add paths raced. `ng-diagram`'s built-in `PaletteDropDirective` handles `drop` on `<ng-diagram>` during the target phase and emits a `paletteDropNode` command with `{}` when no `text/plain` payload exists, creating a local node without `data`. The app's parent `(drop)` bubble handler then also sent `editor.addNode.requested`.                             |
| **Fix**                | Add capture-phase native `dragover` / `drop` listeners on `lf-flow-canvas` for Langflower palette MIME. They call the existing app handlers and `stopImmediatePropagation()` before the event reaches `<ng-diagram>`. The server delta add path uses `adapter.updateNodes` + `upsertDiagramNodes` so existing nodes stay in place and the new node carries full `data`. |
| **Design flaw signal** | **Competing drop ownership** — component-level bubble handlers are too late when a child directive owns the same DOM event. Palette drops must have one owner at the capture boundary before ng-diagram can synthesize local nodes.                                                                                                                                     |
| **Regression test**    | pending user confirmation — update `flow-canvas.component.test.ts` settled → delta mocks                                                                                                                                                                                                                                                                                |

### BUG-2026-06-26f — palette drop position ignores viewport transform

| Field                  | Value                                                                                                                                                                                                |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-26                                                                                                                                                                                           |
| **Area**               | ui · flow-canvas                                                                                                                                                                                     |
| **Status**             | fixed                                                                                                                                                                                                |
| **Symptom**            | Dropped node appears far from cursor (often ~500px offset) when canvas is panned/zoomed or after viewport fit.                                                                                       |
| **Repro**              | Pan/zoom canvas → drop node from palette → node lands in wrong flow coordinates.                                                                                                                     |
| **Root cause**         | `handleDrop` sent `clientX/Y - hostRect` (screen-local pixels) as workflow position. ng-diagram stores node positions in **flow space**; must use `NgDiagramViewportService.clientToFlowPosition()`. |
| **Fix**                | Convert drop point with `viewport.clientToFlowPosition({ x: event.clientX, y: event.clientY })` before `editor.addNode.requested`.                                                                   |
| **Design flaw signal** | **Coordinate space mismatch** — DOM/client coords ≠ diagram flow coords whenever viewport metadata is non-identity.                                                                                  |
| **Regression test**    | `packages/ui/src/app/features/canvas/flow-canvas.component.test.ts` — "emits editor.addNode.requested with flow coordinates from viewport"                                                           |

### BUG-2026-06-26g — palette drag preview NG0201 (no ng-diagram providers)

| Field                  | Value                                                                                                                                                                                                                                                                 |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-26                                                                                                                                                                                                                                                            |
| **Area**               | ui · palette · angular di                                                                                                                                                                                                                                             |
| **Status**             | fixed                                                                                                                                                                                                                                                                 |
| **Symptom**            | `NG0201` in browser console when starting palette drag after drag-image preview was wired to `LfNodeComponent`.                                                                                                                                                       |
| **Repro**              | Drag any node from palette sidebar → error before/at drop.                                                                                                                                                                                                            |
| **Root cause**         | `attachPaletteDragImage` uses `createComponent` with root `EnvironmentInjector`. `PaletteDragPreviewComponent` rendered `LfNodeComponent` → `LfNodePortRowComponent` → `NgDiagramPortComponent`, which requires `provideNgDiagram()` scoped to `FlowCanvasComponent`. |
| **Fix**                | Drag preview uses `LfNodePortRowStaticComponent` (CSS port dots, no ng-diagram) with the same `lf-node-chrome` layout and `resolveNodePorts` data.                                                                                                                    |
| **Design flaw signal** | **DI boundary for off-canvas components** — `createComponent` outside the diagram host cannot reuse diagram-internal leaf components without duplicating providers or a provider-free visual clone.                                                                   |
| **Regression test**    | `none` (runtime DI + drag API; manual palette drag smoke)                                                                                                                                                                                                             |

### BUG-2026-06-26h — new WS client reloads workflow from disk, discards in-memory graph

| Field                  | Value                                                                                                                                                                                                                        |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-26                                                                                                                                                                                                                   |
| **Area**               | server · bridge · session                                                                                                                                                                                                    |
| **Status**             | fixed                                                                                                                                                                                                                        |
| **Symptom**            | Second browser tab or page reload shows canvas layout from persisted workflow file, not the live in-memory graph (unsaved moves/adds lost on connect).                                                                       |
| **Repro**              | Tab A: load workflow, move a node (don't save). Tab B connect or reload browser → node at old on-disk position.                                                                                                              |
| **Root cause**         | Each WS client got its own `LangflowerSession`; `emitBootstrap` always called `loadWorkflowIntoSession` from disk; disconnect disposed the runtime.                                                                          |
| **Fix**                | One shared `LangflowerSession` per bridge/server process. Bootstrap loads from disk only when `activeWorkflow === null`; reconnect sends `session.state.snapshot` from memory. Client disconnect no longer disposes session. |
| **Design flaw signal** | **Session scope vs transport scope** — domain state (runtime + active workflow) must outlive a single WebSocket connection; bootstrap is hydrate-or-replay, not always cold-load.                                            |
| **Regression test**    | `tests/integration/ws/ws-session-sync.ws.test.ts` — second client + reconnect snapshots                                                                                                                                      |

### BUG-2026-06-26i — bootstrap viewportChanged overwrites session viewport on server

| Field                  | Value                                                                                                                                                                                             |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-26                                                                                                                                                                                        |
| **Area**               | ui · flow-canvas · viewport sync                                                                                                                                                                  |
| **Status**             | fixed                                                                                                                                                                                             |
| **Symptom**            | After reconnect, server sends correct `session.state.snapshot.viewport`, then client emits `editor.viewport.requested` `{ x: 0, y: 0, scale: 1 }` and clobbers shared session viewport.           |
| **Repro**              | Pan/zoom canvas → reload → snapshot has saved viewport → ng-diagram init fires default `viewportChanged` → WS sends `{0,0,1}`.                                                                    |
| **Root cause**         | Outbound viewport sync was enabled before session viewport hydration finished; init `viewportChanged` was treated as user edit.                                                                   |
| **Fix**                | Bootstrap gate: ignore outbound sends until session viewport is applied (or confirmed absent). Skip default `{0,0,1}` during hydration; consume first post-`setViewport` echo without publishing. |
| **Design flaw signal** | **Init events ≠ user intent** — diagram lifecycle emits default viewport before server state is applied; domain intents must wait for hydrate-complete boundary.                                  |
| **Regression test**    | `packages/ui/src/app/features/canvas/canvas-viewport-sync.test.ts`; manual reconnect smoke                                                                                                        |

### BUG-2026-06-26j — Save stays disabled after editor mutations (no dirty broadcast)

| Field                  | Value                                                                                                                                                                                  |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-26                                                                                                                                                                             |
| **Area**               | server · bridge · ui · workflow topbar                                                                                                                                                 |
| **Status**             | fixed                                                                                                                                                                                  |
| **Symptom**            | Save button inactive after add/move/connect nodes — `currentStatus` stays `pristine` in UI.                                                                                            |
| **Repro**              | Load workflow → add node or move node → Save disabled.                                                                                                                                 |
| **Root cause**         | Editor delta rollout removed `workflow.current.snapshot` broadcasts from editor handlers (to avoid canvas resnapshot). Server called `markDirty()` but never pushed status to clients. |
| **Fix**                | New `workflow.currentStatus.snapshot` broadcast after non-empty `editor.*.delta`; topbar applies status slice without touching canvas.                                                 |
| **Design flaw signal** | **Status vs graph transport** — dirty flag is workflow-manager state, not canvas topology; must not require full `workflow.current.snapshot` (which would re-merge the graph).         |
| **Regression test**    | `tests/integration/ws/editor.ws.test.ts` — add node emits `{ status: 'dirty' }`                                                                                                        |

### BUG-2026-06-26k — addEdge to occupied port returns empty delta

| Field                  | Value                                                                                                                                                                                                                                                                                                          |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-06-26                                                                                                                                                                                                                                                                                                     |
| **Area**               | server · bridge · ws · editor                                                                                                                                                                                                                                                                                  |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                          |
| **Symptom**            | Drawing a new wire to an already-connected input port yields `editor.addEdge.delta` `[]`; old edge stays, new connection never appears.                                                                                                                                                                        |
| **Repro**              | Connect `string-1 → preview.text`, then connect `string-2 → preview.text` — WS shows empty add delta.                                                                                                                                                                                                          |
| **Root cause**         | `RuntimeEditor.addEdge` rejects occupied target slots by design; server `applyEditorAddEdge` returned `[]` without removing the incumbent edge first.                                                                                                                                                          |
| **Fix**                | Server replace flow: find edge on target port, `removeEdge` then `addEdge`; bridge emits `editor.removeEdge.delta` then `editor.addEdge.delta`. Runtime unchanged.                                                                                                                                             |
| **Follow-up fix**      | Replace used `applyEditorRemoveEdge`, which runs runtime `cleanupInvalidEdges` and cascaded removal of downstream passthrough wires before the replacement edge was added. `RuntimeEditor.replaceEdge` now performs the atomic swap and rolls back when cleanup would prune any edge.                          |
| **Follow-up fix 2**    | Persisted workflow kept stale edge ids after load (`activeWorkflow` from disk vs runtime UUIDs). Replace removed by runtime id only, so the old wire stayed in `example.json` and a second wire was appended — bind now preserves edge ids; persisted remove matches endpoints; append dedupes by target port. |
| **Design flaw signal** | **Editor policy vs runtime strictness** — canvas replace-on-reconnect is an editor concern; runtime may stay strict while server orchestrates remove-then-add.                                                                                                                                                 |
| **Regression test**    | `packages/server/src/workflow/apply-editor-mutation.test.ts`; `tests/integration/ws/editor.ws.test.ts` — replace on occupied port                                                                                                                                                                              |

### BUG-2026-07-20 — canvas viewport hydrate marks workflow dirty (Save active on load)

| Field                  | Value                                                                                                                                                                                                                                         |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-20                                                                                                                                                                                                                                    |
| **Area**               | ui · flow-canvas · server · workflow topbar                                                                                                                                                                                                   |
| **Status**             | fixed                                                                                                                                                                                                                                         |
| **Symptom**            | Save becomes enabled shortly after loading a pristine workflow, before any user edit.                                                                                                                                                         |
| **Repro**              | Load a saved workflow with non-zero pan → wait ~300ms → Save active / `currentStatus` is `dirty`.                                                                                                                                             |
| **Root cause**         | After mount, ng-diagram re-emits the hydrated viewport; client sent `editor.viewport.requested` (weak `Boolean(v.x && v.y)` gate). Server always wrote viewport + `markDirty()`. Related to BUG-2026-06-26i (hydrate gate never fully wired). |
| **Fix**                | Client: `gateCanvasViewportPublish` + `distinctUntilChanged` before publish. Server: skip write/`markDirty` when payload matches current viewport (epsilon).                                                                                  |
| **Design flaw signal** | **Lifecycle echo treated as mutation** — diagram/service re-emits restored state; domain dirty must require a real change, not a no-op round-trip.                                                                                            |
| **Regression test**    | `packages/ui/.../canvas-viewport-sync.test.ts` (`gateCanvasViewportPublish`); `tests/integration/ws/editor.ws.test.ts` — same viewport echo stays pristine                                                                                    |

### BUG-2026-07-21 — grok-feed timeline order + streaming wall

| Field                  | Value                                                                                                                                                                                                                                |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Date**               | 2026-07-21                                                                                                                                                                                                                           |
| **Area**               | ui · feed · grok-feed                                                                                                                                                                                                                |
| **Status**             | fixed                                                                                                                                                                                                                                |
| **Symptom**            | (1) Settled turn showed result above technical rows — newest chat content not at the bottom. (2) Open reasoning/draft stream showed the full multi-line body while working.                                                          |
| **Repro**              | Run `basic-coder` / any agent with reasoning→result; inspect work log order and open technical block mid-stream.                                                                                                                     |
| **Root cause**         | `rowsForGroup` appended technical after result; streaming body always used full `sectionDisplayText` with `whitespace-pre-wrap`.                                                                                                     |
| **Fix**                | Chronological row order: technical → permission → result. Technical peek = last port in 4-line pane only while active; settled peek = one line (no leftover height). Node label above `details` summary. `<details>` default-closed. |
| **Design flaw signal** | **Projection order ≠ chat chronology** — grouping helpers must emit conversation-time order (oldest top / newest bottom), not “important first”.                                                                                     |
| **Regression test**    | `packages/ui/.../feed-timeline.test.ts` — settled order + last-line streaming preview                                                                                                                                                |

### BUG-2026-07-23b — Clear clears canvas chrome but not the work-log feed

| Field                  | Value                                                                                                                                                                                                                                                                                 |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-23                                                                                                                                                                                                                                                                            |
| **Area**               | ui · feed · executionFeed                                                                                                                                                                                                                                                             |
| **Status**             | fixed                                                                                                                                                                                                                                                                                 |
| **Symptom**            | Work-log Clear removes node/edge highlights but leaves feed rows. Tab reload shows an empty feed (server clear worked).                                                                                                                                                               |
| **Repro**              | Run any workflow → Clear in work log → canvas chrome gone, feed still full.                                                                                                                                                                                                           |
| **Root cause**         | Clear re-broadcasts `executionFeed.snapshot = null` after bootstrap already sent `null`. Feed fold used `combineLatest` + `distinctUntilChanged` on snap identity (`null === null`), so the clear was suppressed while live turns stayed in the scan. Chrome fold has no such filter. |
| **Fix**                | Drive snapshot actions from `executionFeed.snapshot` with `switchMap` + `catalog$.pipe(take(1))` so every feed emission (including repeated null) applies, without re-folding on catalog churn.                                                                                       |
| **Design flaw signal** | **Identity equality on nullable snapshots** — `null` cannot distinguish “idle bootstrap” from “cleared after live appends”. Event-sourcing clear must react to the emission, not only to a new payload identity.                                                                      |
| **Regression test**    | `packages/ui/.../workflow-execution.service.test.ts` — Clear re-emits null after bootstrap null                                                                                                                                                                                       |

### BUG-2026-07-23 — loading sentinel appears as `{}` in feed / WS

| Field                  | Value                                                                                                                                                                                                                                 |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-23                                                                                                                                                                                                                            |
| **Area**               | runtime · ws · feed                                                                                                                                                                                                                   |
| **Status**             | fixed                                                                                                                                                                                                                                 |
| **Symptom**            | Workflow `line` (and others) show a spurious `{}` row in the feed before the real string value; WS `runner.output-emitted` has `state:'value'` and `value:{}`.                                                                        |
| **Repro**              | Run demo-project workflow `line`; inspect feed and WS log for the string node output.                                                                                                                                                 |
| **Root cause**         | `@rx-evo` loading sentinel `{ state: Symbol(loading) }` was misclassified as success when CJS/ESM dual-package instances made `isLoading` return false. JSON drops the Symbol → `{}` on the bus; Preview stringifies it for the feed. |
| **Fix**                | Structural `isSymbolStateSentinel` (`{ state: Symbol }`) in `resolveSignalState` / edge filter / `isConnectionInactive`; `filter(isEdgeForwardable)` so sentinel/error never forward on edges (see also ADR-028).                     |
| **Design flaw signal** | **Identity of Symbols across dual packages** — status guards that rely on Symbol identity are unsafe when the same package can load twice (CJS vs ESM). Prefer structural checks or a single resolution path for `@rx-evo`.           |
| **Regression test**    | `packages/runtime/src/testing/workflows/empty-object-string.workflow.test.ts`                                                                                                                                                         |

### BUG-2026-06-28 — bootstrap load fails when passthrough outbound edge precedes inbound

| Field                  | Value                                                                                                                                                                                                                                                                                                                          |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Date**               | 2026-06-28                                                                                                                                                                                                                                                                                                                     |
| **Area**               | runtime · server · ws                                                                                                                                                                                                                                                                                                          |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                          |
| **Symptom**            | Client receives `session.state.snapshot` with `activeWorkflow: null` despite `currentWorkflowId: "example"`; canvas empty on load/reload.                                                                                                                                                                                      |
| **Repro**              | Start server on `demo-project` → connect WS → snapshot has catalog but no graph. `loadWorkflowIntoSession('example')` returns false.                                                                                                                                                                                           |
| **Root cause**         | `bindWorkflowToSessionEditor` adds persisted edges in file order. Passthrough outputs (`inferTypeFrom`) resolve wire type via `resolveInputPinnedWireType`, which only looked at upstream edges — not static input `wireType`. Outbound edges before inbound failed validation; bind aborted without setting `activeWorkflow`. |
| **Fix**                | `RuntimeEditor.resolveInputPinnedWireType` falls back to static input `wireType` when no upstream edge exists (dynamic inputs unchanged).                                                                                                                                                                                      |
| **Design flaw signal** | **Bind order vs wire validation** — persisted graph edge order must not affect load success; static port types are declared on the node definition.                                                                                                                                                                            |
| **Regression test**    | `packages/runtime/src/runtime.test.ts` — passthrough outbound before inbound; manual: demo-project `example` bind                                                                                                                                                                                                              |

### BUG-2026-07-27 — provider 500 or hung stream kills LLM recovery

| Field                  | Value                                                                                                                                                                                                                                                                                     |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-27                                                                                                                                                                                                                                                                                |
| **Area**               | common-nodes · LLM · RxJS · provider recovery                                                                                                                                                                                                                                             |
| **Status**             | fixed                                                                                                                                                                                                                                                                                     |
| **Symptom**            | A provider HTTP 500 (often an HTML body from LM Studio) terminates the LLM node; subsequent Pause/Steer or interrupt + go cannot recover. A provider that stops emitting can wait forever.                                                                                                |
| **Repro**              | Run a multi-turn OpenAI-compatible workflow; make the provider throw `{ status: 500, message: "<html>…" }` or return a stream that never emits/completes; try Pause then Steer.                                                                                                           |
| **Root cause**         | Two imperative async-IIFE Observable bodies consumed provider streams with `for await`; exceptions called `subscriber.error`, permanently terminating the StatefulObservable cycle, while no idle boundary existed. Session history/counters were separately mutated in `tap` / closures. |
| **Fix**                | Shared `runLlmLoop` `expand` machine + typed provider RxJS facts, idle watchdog, checkpointed retries/suspension, sanitized diagnostics, bounded sequential tool/Sub-Agent effects; shared `runLlmSessionMachine` `mergeScan` owns history and queued turns.                              |
| **Design flaw signal** | **Recoverable domain failure encoded as stream death** — external provider availability is a loop fact, not automatically an Observable terminal error. Commit history only at explicit checkpoints and reserve the error lane for fatal authentication/configuration/protocol failures.  |
| **Regression test**    | `ai/features/llm-loop/operators/observe-provider-stream.test.ts` (idle/Pause/500), `ai/features/llm-loop/run-agent-loop.test.ts` (500 → Steer → continue), `ai/features/llm-session/run-session-machine.test.ts` (queued immutable history), migrated Review/tool-loop tests.             |

### BUG-2026-07-28 — double stream.done orphans tool_calls and breaks compaction

| Field                  | Value                                                                                                                                                                                                                                                                                                                                                     |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Date**               | 2026-07-28                                                                                                                                                                                                                                                                                                                                                |
| **Area**               | common-nodes · LLM · tool loop · context compaction                                                                                                                                                                                                                                                                                                       |
| **Status**             | fixed                                                                                                                                                                                                                                                                                                                                                     |
| **Symptom**            | Feed shows `Provider failure: Cannot compact history: incomplete assistant tool_calls / tool result block`, retries 1/2 and 2/2, then pauses for Steer/Resume. Resume often fails the same way.                                                                                                                                                           |
| **Repro**              | OpenAI-compatible agent tool round that later exceeds `contextSize` and triggers proactive compaction (or seed history with an incomplete assistant/`tool_calls` block).                                                                                                                                                                                  |
| **Root cause**         | On `provider.done` with `tool_calls`, `providerFactAction` reduced `stream.done` in the scan **and** `decisionPackets` `run-tools` reduced `stream.done` again. Tools closed only the second assistant block; the first orphan stayed incomplete and failed `toBlocks`. Compaction errors were classified as recoverable `unknown` and uselessly retried. |
| **Fix**                | Scan skips `stream.done` when tool calls are present (policy owns the sole commit); path-choice builds synthetic history from `roundCheckpoint`; empty-pending tools roll back to checkpoint; `Cannot compact history:` → `protocol` with Steer and no transient retries.                                                                                 |
| **Design flaw signal** | **Duplicate reduce of the same provider fact** — scan + policy must not both append the same assistant/`tool_calls` block; structural history failures are not transient provider outages.                                                                                                                                                                |
| **Regression test**    | `ai/features/llm-loop/run-agent-loop.test.ts` (single closed tool block; compaction protocol → Steer without retries), `ai/features/llm-loop/llm-loop-reducer.test.ts` (classify), `ai/features/path-choice/run-path-choice-tool-loop.test.ts` (historySync pair), `ai/features/openai/llm-context-compaction.test.ts` (incomplete block reject)          |
