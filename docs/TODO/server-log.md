# Server bridge logs — done

Shipped: always-on (configurable) bridge JSONL diagnostics under
`.langflower/logs/`. Toggle via Settings **Server logs** (Off / Default / On)
or `serverLogs` boolean in project/global `langflower.jsonc`. See
`packages/server/src/bridge/BRIDGE.md` and `docs/CONFIG.md`.
