# String

|              |                 |
| ------------ | --------------- |
| **Type**     | `common-string` |
| **Category** | Primitives      |

## Summary

String literal from the inline `value` input port → output `value`.

## Inputs

`value` (string, inline: text, default '')

## Outputs

`value` (string)

---

_Implementation removed — re-add via `defineReactiveNode` + `bind()` API._
