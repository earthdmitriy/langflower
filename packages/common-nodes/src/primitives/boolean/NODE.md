# Boolean

|              |                  |
| ------------ | ---------------- |
| **Type**     | `common-boolean` |
| **Category** | Primitives       |

## Summary

Boolean literal from the inline `value` input port → output `value`.

## Inputs

`value` (boolean, inline: boolean, default false)

## Outputs

`value` (boolean)

---

_Implementation removed — re-add via `defineReactiveNode` + `bind()` API._
