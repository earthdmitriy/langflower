# String (multiline)

|              |                           |
| ------------ | ------------------------- |
| **Type**     | `common-string-multiline` |
| **Category** | Primitives                |

## Summary

Multiline string literal from the inline `value` textarea → output `value`.

## Inputs

`value` (string, inline: text-multiline, default '')

## Outputs

`value` (string)

---

_Implemented via `defineReactiveNode` + `bind()` (`src/primitives/string-multiline/node.ts`)._
