import { Routes } from '@angular/router';
import { EditorShellComponent } from './features/editor/components/editor-shell.component';

export const routes: Routes = [
	{
		path: '',
		component: EditorShellComponent,
	},
];
