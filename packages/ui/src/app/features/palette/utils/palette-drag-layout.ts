/** Matches `rounded-xl` on lf-node chrome — drag anchor offset from top-left. */
export const PALETTE_DRAG_ANCHOR_OFFSET_PX = 12;

/** Uniform drag ghost opacity (entire preview). */
export const PALETTE_DRAG_OPACITY = 0.5;
